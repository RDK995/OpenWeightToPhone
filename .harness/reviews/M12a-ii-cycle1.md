# M12a-ii — Cycle 1 Review

Reviewer tier: Top (`opus`). Scope: the whole milestone.

I ran every validation independently and performed five source mutations myself. The tree is restored to its recorded hashes (`main.ts` = `24743340…`, `credential-store.ts` = `e8e8b44f…`, `pairing-target.ts` = `e58f0e71…`).

## Validation reproduced

| Command | Claimed | Observed by me |
| --- | --- | --- |
| `bun test` | exit 0, 833 pass / 0 fail, 32 files | exit 0, **833 pass / 0 fail, 6136 expects, 32 files** |
| `bunx tsc --noEmit -p tsconfig.json` | exit 0 | **exit 0** (tsconfig `include` covers `test`) |
| `bun run build` | exit 0 | **exit 0** |
| `bun run proof:m12a` | exit 0, live | **exit 0**, 3 real profiles from `https://ryans-mac-studio.tailc3648a.ts.net`, corrupted token → `401 unauthorized` |

## Acceptance criteria

**Criterion 1** — pairing view when unpaired; assertion must fail if the view is removed
Implementation: `/Users/ryankenny/Projects/phoneToLocalModel/web/src/main.ts:255-259`, `/Users/ryankenny/Projects/phoneToLocalModel/web/src/ui/pairing-target.ts`
Test: `/Users/ryankenny/Projects/phoneToLocalModel/test/web/start-app.test.ts:104` (`AC1`)
Detector re-run by me: I replaced the branch with unconditional `enterPaired()` — `AC1` and 5 others went red. The detector bites.
Result: **PASS**

**Criterion 2** — full pairing URL stores token + that origin; bare token stores against the current origin; both leave the app paired
Implementation: `web/src/credential-store.ts:122-163`, `web/src/main.ts:232-245` (correct — I confirmed with a scratch test that `submitPairing("tok-bare-1234")` stores `{baseUrl: ORIGIN, token: "tok-bare-1234"}`)
Test: full-URL half proven at app level (`test/web/start-app.test.ts:161`, `:186`) and unit level (`test/web/credential-store.test.ts:502`). Bare-token half proven **only** at the function level (`test/web/credential-store.test.ts:519`) and in `src/host/m12a-proof.ts:100`, which calls `adoptCredentialFromPastedText` directly with `baseUrl` — never through `startApp`.
Mutation by me: `web/src/main.ts:236` `location.origin` → `"https://WRONG.example"` ⇒ **833 pass / 0 fail, whole suite green.**
Result: **FAIL** (see IMPORTANT 1)

**Criterion 3** — proceeds to conversation UI without reload; stays paired across relaunch
Implementation: `web/src/main.ts:218-245`
Test: `test/web/start-app.test.ts:161`, `:186` (no reload), `:231` (`AC5`, fresh `Window`+root over the same `StoragePort`)
Vacuity check re-done by me: I made the already-paired path call `enterUnpaired(null)` before `enterPaired()`. Every final-DOM assertion still passed; `AC3` and `AC5` failed **only** on `expect(painted).not.toContain("pairing")` (received `["pairing","(none)",…]`). The `recordPaintedTestIds` instrumentation is real and the reasoning holds — both views do call `root.replaceChildren`, so this is the only assertion that could distinguish the two cases.
Result: **PASS**

**Criterion 4** — token never reaches URL, title, or anything carried into a bookmark/app switcher; input does not retain it
Implementation: `web/src/ui/pairing-target.ts` — `createElement`/`textContent` only, never sets the `value` attribute, never touches `doc.title` or `location`; no wrapping `<form>` (`web/public/index.html:32` is a bare `<main id="app">`), so the button cannot navigate
Test: `test/web/ui/pairing-target.test.ts:115-174` (attribute sweep, `outerHTML`, `innerHTML`, `doc.title`, input cleared on success), `test/web/start-app.test.ts:363`, `:382`
**The `doc.title` assertion the record flagged as unproven is real.** I ran the missing mutation: added `doc.title = trimmedValue;` to the click handler in `pairing-target.ts` ⇒ both AC6 leak tests went red. The recorded follow-up can be closed.
Result: **PASS**

**Criterion 5** — rejected token surfaces `unauthorized` guidance and returns to the pairing view, not a dead end
Implementation: `web/src/main.ts:132-135` (guarded dispatch, matched on `error.guidance.code`), `:250-253`
Test: `test/web/start-app.test.ts:262` (`AC6`, including recovery), `:301` (`AC7`), `:336` (`AC8`), `:391-438`
Mutations by me: dropping `onUnauthorized` from `enterPaired` ⇒ AC6+AC7 red; deleting `credentialStore.clear()` ⇒ AC6 red.
Result: **PASS**

**Criterion 6** — real token → working authenticated client, at a Mac-run entry point
Implementation/proof: `/Users/ryankenny/Projects/phoneToLocalModel/src/host/m12a-proof.ts`
Evidence generated by me: `bun run proof:m12a` exit 0 against the live harness — real `readToken()`, both pasted-URL (with deliberately wrong `currentOrigin`, pasted origin wins) and bare-token forms through the real adopter, unmocked `listProfiles()` returning 3 profiles, and a corrupted credential observed rejected `401 unauthorized` (fails closed). Token-leak guard clean.
Result: **PASS**

**Criterion 7** — human attestation on `iphone-15-pro`
Result: **PENDING-HUMAN** (correctly unchecked)

## Findings

```
Severity: IMPORTANT

Problem:
Criterion 2's bare-token half is not proven at the level the criterion states it.
No test drives a bare token through startApp, so nothing verifies that startApp
supplies the app's own origin as `currentOrigin`. The behaviour is correct today,
but the wiring is undefended.

Evidence:
/Users/ryankenny/Projects/phoneToLocalModel/web/src/main.ts:233-237
  const credential = adoptCredentialFromPastedText(
    credentialStore,
    pastedText,
    location.origin
  );
Mutating line 236 to a literal "https://WRONG.example" leaves the entire suite at
833 pass / 0 fail. Every startApp test (test/web/start-app.test.ts:161, :186, :231,
:262, :301, :363) pastes a full URL, whose own origin wins and masks this argument;
the only bare-token tests are at the function level
(test/web/credential-store.test.ts:519) and in src/host/m12a-proof.ts:100, which
calls adoptCredentialFromPastedText directly with an explicit baseUrl.

Why it matters:
This is exactly the failure class the project has already recorded twice ("evidence
that an action was invoked is not evidence that it took effect"). A wrong origin
here stores a credential against a baseUrl the phone cannot reach, and the symptom
on the device is an unexplained pairing that immediately 401s — the very defect
this milestone exists to remove. The installed home-screen app has no address bar,
so a user who hits it has no recovery route.

Suggested correction:
Add one test to test/web/start-app.test.ts alongside the AC4 group: from the
unpaired state, `handle.submitPairing("tok-bare-1234")` (or set that value on
[data-testid=pairing-input] and click submit), then assert
`createCredentialStore(storage).getCredential()` equals
`{ baseUrl: ORIGIN, token: "tok-bare-1234" }` and `handle.paired` is true. Confirm
it fails when line 236 is replaced with a wrong-origin literal.
```

```
Severity: OPTIONAL

Problem:
No test proves the shipped bundle's entry path renders the pairing view. The device
runs web/dist/main.js, not the source.

Evidence:
/Users/ryankenny/Projects/phoneToLocalModel/test/web/bundle-fragment.test.ts and
test/web/ui/bundle-interactive.test.ts both import the built bundle but exercise the
exported `bootstrap`, not the `typeof document !== "undefined"` guard that now calls
`startApp`. I verified by hand that the guard and `startApp(root)` do survive into
web/dist/main.js, and that "Pair this app with your Mac" is present.

Why it matters:
bundle-fragment.test.ts exists precisely because M11 lost the fragment-adoption call
to tree-shaking on the way to the bundle. Skipping the equivalent guard here departs
from the pattern the project established for that defect class. Risk is genuinely low
(startApp is an explicit named export and the guard is a top-level side effect), which
is why this is OPTIONAL rather than IMPORTANT.

Suggested correction:
Extend test/web/bundle-fragment.test.ts (or add a sibling) that imports the built
bundle and calls its exported `startApp` with memory storage and an empty-hash
LocationPort, asserting [data-testid=pairing-input] is rendered.
```

```
Severity: OPTIONAL

Problem:
enterPaired() destroys the pairing view before constructing the conversation UI, so a
synchronous throw from bootstrap() leaves the root empty with no view and no way back.

Evidence:
/Users/ryankenny/Projects/phoneToLocalModel/web/src/main.ts:218-221
  function enterPaired(): void {
    releasePairingTarget();
    mountHandle = bootstrap(root, { ...deps, storage, location, onUnauthorized });
  }
releasePairingTarget() calls destroy() -> section.remove(). If bootstrap throws,
mountHandle stays null and pairingTarget is null; pairing-target.ts:65-68 then writes
SUBMIT_THREW_MESSAGE into a detached node. Because adoptCredentialFromPastedText has
already persisted the credential, a relaunch takes the same branch and throws again.

Why it matters:
The result is a permanently blank installed app with no address bar and no control to
touch. I could not construct a realistic trigger (createApiClient at
web/src/api-client.ts:380 does not validate its baseUrl), so this is latent, not live.

Suggested correction:
Wrap the bootstrap() call in enterPaired() in a try/catch that falls back to
enterUnpaired(<message>) and clears the credential, or construct the new view before
releasing the old one.
```

```
Severity: OPTIONAL

Problem:
The milestone's ### Baseline, which is the only usable substitute for a git diff here,
is incomplete and internally inconsistent.

Evidence:
.harness/milestones.md ### Baseline names only web/src/credential-store.ts and
web/src/main.ts as pre-existing files modified. But
/Users/ryankenny/Projects/phoneToLocalModel/web/src/ui/dom-target.ts:1-7 now carries
the rewritten "One of the two modules in web/src/ permitted to touch the DOM" header —
a change sanctioned by M12a-ii-T2's `Files Allowed To Change` ("comment only") but
absent from the milestone record, with no hash. Separately, the record annotates
credential-store.ts as "(120 lines)" while the file at the recorded hash
e8e8b44f… is 163 lines.

Why it matters:
Untracked-at-baseline files make this record the only scoping aid a fresh reviewer has.
An omitted modified file and a line count that contradicts its own hash both cost review
time and could hide a real change next time.

Suggested correction:
Add web/src/ui/dom-target.ts to ### Baseline's list of modified pre-existing files with
its SHA-256, and correct the credential-store.ts line count to match the hashed content.
```

```
Severity: OPTIONAL

Problem:
The second architecture deviation justifies itself with an edge the new code does not use.

Evidence:
.harness/architecture.md, "M12a-ii — C10 now has two DOM-touching modules, not one":
"The agreed diagram edge `C10 --> C5` labelled 'pairing state' already covers the UI
reaching the credential store". web/src/ui/pairing-target.ts never imports or reaches C5 —
it takes an `onSubmit` callback (pairing-target.ts:9-11). The component that reaches C5 is
C12 (web/src/main.ts:197, :202, :233, :251), an edge already declared by the M11 deviation
entry at architecture.md:299-307.

Why it matters:
The substantive conclusion ("no new edge, no new dependency, Material: no") is correct and
I confirmed it, but this is the same class of misquoted-citation defect a prior cycle
already flagged on the M12a-i entry, and it makes the record slightly harder to trust.

Suggested correction:
Replace the C10 --> C5 sentence with the accurate one: pairing-target.ts is deliberately
credential-free (callback-only), and the C12 -> C5 wiring startApp uses is already recorded
in the M11 deviation entry.
```

## Positives worth stating

- **The T1 near-miss is closed.** `bunx tsc --noEmit` covers `src`, `web/src`, `scripts` and `test` and exits 0, so no `TS2554` silently-discarded matcher argument survives anywhere in the suite. The four messages were preserved as `//` comments above their assertions (`test/web/credential-store.test.ts:715-742`).
- **The follow-up requiring reuse rather than a second source of truth is satisfied.** Both branches of `adoptCredentialFromPastedText` build a synthetic `LocationPort` and delegate to `adoptCredentialFromFragment` (`web/src/credential-store.ts:147`, `:162`), guarded by an anti-drift structural test that forbids `store.setCredential` and `URLSearchParams` inside that function.
- **Bundle purity holds.** `happy-dom` appears zero times in `web/dist/main.js` and `web/dist/sw.js`; the built bundle references no `node_modules`. `test/web/bundle-purity.test.ts` enforces it.
- **Both `## Deviations` entries describe what was actually built, and `Material: no` is right.** C5 gained a second capture path that delegates to the single existing parser, at the same `Location:`, with the same dependency (`C11`) and the same ownership. C10 gained a second module inside its agreed `web/src/ui/` location, with `Depends on: C5, C8, C9` unchanged and the DOM-free split of `view-model.ts`/`actions.ts`/`mount.ts` still structurally enforced. No boundary moved, no technology choice changed, no responsibility changed owner.
- **`bootstrap()` is genuinely unchanged in name, signature and behaviour** for every caller that supplies no `onUnauthorized`, which is why 793 → 833 tests grew with zero existing tests modified.

## Verdict

**CHANGES REQUIRED** — 1 IMPORTANT (criterion 2 FAIL). Criteria 1, 3, 4, 5 and 6 all PASS with detectors I verified bite; criterion 7 is PENDING-HUMAN.

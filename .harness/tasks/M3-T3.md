TASK

Goal:
Add unit tests pinning two behaviours M3 depends on: that a `400 unknown_profile` from the
service surfaces as its own typed error all the way out of the Session Coordinator, and that
the coordinator reads a conversation's `profileId` at send time so that changing it takes
effect on the very next generation.

Tier: Cheap. Routing reason: none — both behaviours are already specified, the blast radius is
two test files, and each is settled deterministically by `bun test` with a fetch stub.

This is a test-only task. The current implementation is expected to already satisfy both
behaviours. Write the tests first and run them. If they pass immediately, say so explicitly in
your return — that is a legitimate outcome and you must NOT invent a change to make it look
like Red -> Green. If either goes red, make the smallest correct fix in the named source file
and report exactly what was wrong.

Relevant Requirements:
- FR9 (Error surfacing): documented error codes, including `unknown_profile`, are surfaced
  meaningfully rather than as a generic failure.
- FR5 (Profiles): profile is selectable per conversation and changeable.
- Milestone M3 criteria: "A profile id the service does not know surfaces `unknown_profile`
  (`400`) as its own typed error" and "A conversation's profile can be changed, and the next
  generation runs under the new profile".

Acceptance Criteria:

A. In `test/web/api-client.test.ts`:
   - `generate` against a stub returning HTTP 400 with body `{"api_version":"v1","error":"unknown_profile"}`
     throws `HarnessApiError` with `code === "unknown_profile"` and `status === 400`.
   - Assert on the thrown error's fields, not merely that something threw.
   - Assert specifically that the code is NOT the generic `http_400` fallback. This is the
     substance of "as its own typed error" and must be its own assertion.

B. In `test/web/session-coordinator.test.ts`:
   - When the api client's `generate` rejects with `HarnessApiError("unknown_profile", 400)`,
     `coordinator.send(...)` rejects with that SAME error object — not a rewrapped or
     generic one. Assert identity or at minimum that `code` and `status` survive intact.
   - Assert the store's pending generation is not left set after that failure.
   - The coordinator passes the conversation's CURRENT `profileId` to `apiClient.generate`:
     create a conversation with one profile id, send, capture the `profileId` the fake client
     received; then change the conversation's profile via the store's `setProfileId`, send
     again, and assert the fake client received the NEW id on the second call. This must fail
     if the coordinator ever caches the profile id, so capture both calls and assert on both.

Relevant Files:
- `web/src/api-client.ts` — `handleResponse` builds the error code; read it.
- `web/src/session-coordinator.ts` — note that `apiClient.generate` is called OUTSIDE the
  try/catch, which is why the typed error currently propagates unwrapped. Your test pins that.
- `test/web/api-client.test.ts` and `test/web/session-coordinator.test.ts` — follow the
  existing stubbing style in each.
- `web/src/conversation-store.ts` — `setProfileId` already exists.

Files Allowed To Change:
- `test/web/api-client.test.ts`
- `test/web/session-coordinator.test.ts`
- `web/src/api-client.ts` (only if a test legitimately goes red)
- `web/src/session-coordinator.ts` (only if a test legitimately goes red)

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies.
- Do not weaken tests. Do not delete or relax any existing assertion.
- **Do not hardcode any real profile id** (`reasoning-baseline`, `reasoning-capable`,
  `reasoning-deep`) anywhere. Use placeholders. A repository-wide guard test enforces this for
  source files; honour it in tests too.
- Keep `web/src` clean under `bunx tsc --noEmit`. No `!` assertions, no `as any`.

Tests:
export PATH="$HOME/.bun/bin:$PATH" && bun test test/web/api-client.test.ts test/web/session-coordinator.test.ts

Return:
- Summary
- Files changed
- Tests run
- Test result
- Whether each new test passed on first run or went red first (state this explicitly per test)
- Unresolved issues

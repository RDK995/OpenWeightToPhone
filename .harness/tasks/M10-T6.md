TASK

Tier: Mid. Named reason: **not low risk** — it changes the behaviour of the installed
launchd agent supervising a live, human-facing service and re-runs the live proof that
kills and restarts it.

Previous Attempt(s):
- M10-T5 (Mid, attempt 3) wrote `src/host/m10-proof.ts` and ran it. Phases 1 and 2 PASS.
  Phase 3 FAILED, reproducibly, across five full runs plus one isolated 6+ minute
  observation: after `kill -9` of the agent's process, launchd did not automatically respawn
  it within 45s (or at all). `launchctl print gui/501/com.ryankenny.phone-pwa` reports
  `pended nondemand spawn = inefficient`. An explicit `launchctl kickstart -k` always
  restores the process in ~1-2s, so `installLaunchAgent()`, the plist and the asset server
  are all correctly wired — only the AUTOMATIC respawn is being deferred by the OS.
  T5's diagnosis, which you should treat as a lead rather than as fact: `ProcessType`
  `Background` marks the job as throttleable/deferrable background work on macOS 26.6.1, so
  launchd declines to perform the non-demand respawn promptly. T5 was NOT permitted to
  change `launch-agent.ts` behaviour, so it correctly reported the failure instead of
  weakening the proof. You ARE permitted, and that is the point of this task.

Goal:
Make the installed launch agent actually restart the asset server promptly after the
process dies, and then re-run the M10 proof to green.

Relevant Requirements:
FR2. Component C13 — "Keep the asset server running from login onwards, and restart it if
it exits." Architecture interface `C13 -> C4`: "A launchd property list invoking the asset
server, with `KeepAlive` and `RunAtLoad`." Note that `ProcessType` is NOT part of that
agreed interface, so changing or removing it is an implementation choice inside C13 and
needs no architecture deviation. `KeepAlive` and `RunAtLoad` must both remain.

Acceptance Criteria:
- Change `renderPlist` in `src/host/launch-agent.ts` so the agent is not deferred. Work
  through these in order and STOP at the first that makes the live restart happen within
  45s, reporting which one you settled on and what you observed for each you tried:
    1. `ProcessType` set to `Standard` instead of `Background`.
    2. Omit the `ProcessType` key entirely.
    3. `ProcessType` set to `Interactive`.
  If none works, do NOT fake it: report what you observed for all three and return BLOCKED
  with the evidence. Adding an explicit small `ThrottleInterval` is permitted if it helps
  (launchd's default respawn throttle is 10s, comfortably inside the 45s window), but it is
  not a substitute for fixing the deferral.
- `KeepAlive` stays unconditional `<true/>` and `RunAtLoad` stays `<true/>`. Do not convert
  KeepAlive to a dict — the criterion requires a restart after ANY exit, including SIGKILL.
- Update `test/host/launch-agent.test.ts` to match: its round-trip assertion currently
  expects `ProcessType === "Background"`. Change that assertion to whatever the plist now
  declares, or, if you removed the key, assert it is absent. This is a correction to keep
  the test truthful, not a weakening — every other assertion in that file stays exactly as
  it is, and the `plutil -lint` oracle must still run and pass.
- `bun run proof:m10` must now exit 0 and print `M10 LIVE PROOF: PASS`, with phase 3
  showing PID_2 different from PID_1 and the tailnet origin returning 200 at the end.
  Re-running `installLaunchAgent` as part of the proof is expected — it is idempotent.
- Do NOT relax anything in `src/host/m10-proof.ts`: not the 45s window, not the
  PID_2 != PID_1 assertion, not the tailnet 200 check, not the phase 2 discriminating
  control. If you touch that file at all, it must be to fix a genuine bug in it, and you
  must say exactly what and why in your return. The proof is the milestone's evidence; a
  proof edited to pass is worth less than a red one.
- Leave the production agent installed, loaded and serving at the end, and confirm it:
  `curl` the tailnet origin `/app/` and `/app/app.webmanifest` and show 200 for both.

Relevant Files:
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/launch-agent.ts (renderPlist)
- /Users/ryankenny/Projects/phoneToLocalModel/test/host/launch-agent.test.ts (the assertion to correct)
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/m10-proof.ts (the proof — read it, do not weaken it)

Files Allowed To Change:
- src/host/launch-agent.ts
- test/host/launch-agent.test.ts
- src/host/m10-proof.ts (ONLY to fix a genuine bug, explained in your return)

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required.
- Do not weaken tests, and do not weaken the proof.
- Do not touch web/, scripts/, deploy/ or package.json.

Tests:
Bun is NOT on PATH in non-interactive shells and Node is not installed. Export PATH first.

  export PATH="$HOME/.bun/bin:$PATH"
  cd /Users/ryankenny/Projects/phoneToLocalModel
  bun test test/host/launch-agent.test.ts
  bunx tsc --noEmit
  bun test
  bun run build
  bun run proof:m10

`bun test` must report 0 failures (currently 521 pass / 0 fail across 19 files) and
`bun run proof:m10` must exit 0 printing `M10 LIVE PROOF: PASS`.
Paste into your return the COMPLETE stdout of the final `bun run proof:m10` (it is the
milestone's evidence), the final rendered plist, and:
  launchctl list | grep -i phone
  curl -sS -o /dev/null -w '%{http_code} %{content_type}\n' https://ryans-mac-studio.tailc3648a.ts.net/app/
  curl -sS -o /dev/null -w '%{http_code} %{content_type}\n' https://ryans-mac-studio.tailc3648a.ts.net/app/app.webmanifest

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

TASK  (M9-T1 — the Session Coordinator gains the profile surface the UI needs)

Tier: Cheap. Reason for tier: none — two thin delegations onto collaborators that already
exist, with an exact stated signature and a direct unit-test oracle.

Goal:
Add two methods to the `SessionCoordinator` interface and its implementation in
`web/src/session-coordinator.ts`, so that the UI layer (built in later tasks) can list and
change profiles **through the coordinator** rather than reaching past it.

Background you need, and must not exceed:
The architecture gives component C10 (UI) dependencies on C5, C8 and C9 — it has NO
dependency on C7 (the api client). So the UI cannot call `apiClient.listProfiles()`
itself; C9 (this coordinator) must expose it. And milestone M9's acceptance criterion 3
requires the "choose profile" action to go through the coordinator, so C9 must expose a
profile setter too. Both are thin pass-throughs onto collaborators the coordinator already
holds. You are NOT redesigning anything: add exactly these two methods and nothing else.

Relevant Requirements:
- FR5 — Profiles: "Fetch `/v1/profiles` at runtime. Profile ids are never hardcoded."
  and "Profile is selectable per conversation and changeable."

Acceptance Criteria:
- `SessionCoordinator` (the exported interface in `web/src/session-coordinator.ts`) gains
  exactly these two members, with exactly these signatures:
  ```ts
  listProfiles(): Promise<Profile[]>;
  setProfile(conversationId: string, profileId: string): Promise<Conversation>;
  ```
  `Profile` is imported as a type from `./api-client`; `Conversation` is already imported
  from `./conversation-store`.
- `createSessionCoordinator` implements them as one-to-one delegations:
  - `listProfiles()` calls `deps.apiClient.listProfiles()` exactly once, with no
    arguments, and returns its resolved value unchanged. It does NOT cache, filter, sort,
    or fall back to any hardcoded profile id.
  - `setProfile(conversationId, profileId)` calls
    `deps.conversationStore.setProfileId(conversationId, profileId)` exactly once, with
    those two arguments in that order, and returns the resulting `Conversation`. It does
    NOT touch the api client and does NOT create or mutate a session.
  - Neither swallows an error: a rejection or throw from the collaborator propagates to
    the caller unchanged.
- New tests are added to `test/web/session-coordinator.test.ts` proving each of the above
  with recording fakes: the delegation happens, the arguments are exactly forwarded, the
  return value is passed through unchanged, and a collaborator error propagates. Follow the
  fake/stub style already used in that file — read it first and reuse its existing helpers
  rather than inventing a new fake shape.
- Every existing test in the repository still passes. In particular
  `test/no-hardcoded-profile-ids.test.ts` must still pass — do not write a literal profile
  id anywhere, including in a default, a comment example, or a test fixture that the
  scanner would catch. Read that test before you start so you know what it scans.

Relevant Files:
- `web/src/session-coordinator.ts` — `SessionCoordinator` interface at ~line 49;
  `createSessionCoordinator` at ~line 200. Read both in full.
- `web/src/api-client.ts` — `Profile` interface at ~line 311; `ApiClient.listProfiles` at
  ~line 352.
- `web/src/conversation-store.ts` — `ConversationStore.setProfileId` at ~line 96.
- `test/web/session-coordinator.test.ts` — existing tests and fakes.
- `test/no-hardcoded-profile-ids.test.ts` — the guard you must not trip.

Files Allowed To Change:
- `web/src/session-coordinator.ts`
- `test/web/session-coordinator.test.ts`

Tests:
`export PATH="$HOME/.bun/bin:$PATH"; bun test test/web/session-coordinator.test.ts test/no-hardcoded-profile-ids.test.ts && bunx tsc --noEmit && bun test`
All must exit 0. Report the pass/fail counts you actually saw.

Environment (read this first, it bites every task):
- Bun 1.4.0 is at `~/.bun/bin/bun` and is NOT on PATH in non-interactive shells. Node and
  npm are NOT installed. Every command must `export PATH="$HOME/.bun/bin:$PATH"` first.
- STANDING REPOSITORY CONDITION: nearly all source (`web/`, `src/`, `test/`, `scripts/`) is
  UNTRACKED — the repo has only 2 commits. `git diff` does NOT show changes. Inspect the
  working tree directly. Do NOT run `git stash`, `git checkout --`, `git restore`,
  `git reset --hard`, or `git clean` — you share this tree with concurrent workers.
- Repo root: /Users/ryankenny/Projects/phoneToLocalModel

Constraints:
- Follow existing repository patterns (2-space indent in `web/src/**` and `test/web/**`).
- Do not change unrelated behaviour.
- Do not introduce dependencies. No new packages, no DOM test library.
- Do not weaken tests. Do not skip or delete existing tests.
- Do not implement anything outside this packet's Goal. Note good ideas under
  `Unresolved Issues` instead.

Return:
- Summary
- Files Changed
- Tests Run
- Result: PASS | FAIL | BLOCKED
- Unresolved Issues

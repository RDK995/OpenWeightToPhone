TASK

Goal:
Record the M8 architecture deviation in `.harness/architecture.md` under its existing
`## Deviations` section: `SessionCoordinator` (C9) gained a new public method
`createConversation` that the written `C10 -> C9` interface does not name. This closes the
IMPORTANT finding in `.harness/reviews/M8-cycle1.md`. This is a DOCUMENTATION-ONLY task —
change no source code.

Relevant Requirements:
The IMPORTANT finding in `.harness/reviews/M8-cycle1.md` (read it — do not rely on this
packet's paraphrase). Milestone M8 in `.harness/milestones.md` (its section is lines
542-787; its `### Architecture` note is lines 557-567).

The materiality judgement is ALREADY MADE by the orchestrator and is NOT yours to revisit:
this is `Material: no`. Reason, which your entry must convey: `createConversation` is an
entry point over responsibilities C9 already owns — C9 already depends on C7 (whose
documented `C9 -> C7` interface already includes `createSession()`) and on C8 (whose
documented `C9 -> C8` interface already covers conversation persistence). No component
boundary moves, no technology choice changes, and no responsibility changes owner. C8 still
owns the persisted conversations and has no knowledge of the api client. It is recorded
only because the written interface no longer matches what exists.

Acceptance Criteria:
- A new deviation entry is appended at the END of the `## Deviations` section of
  `.harness/architecture.md` (that heading is at line 294; the file currently ends at line
  338, with the M5 entry last). The new entry comes after the existing M5 entry.
- The entry follows the EXACT form of the existing M3 and M5 entries: a bolded lead
  sentence naming the milestone and the deviation, then prose explaining why no boundary,
  technology choice or ownership changes, then a final line reading exactly
  `Material: no`.
- The lead sentence begins with `**M8 — ` and names that C9's `C10 -> C9` surface is wider
  than the interface as written.
- The body states that the Interfaces section's `C10 -> C9` line lists only `send`,
  `cancel`, `resumeIfInterrupted` and a subscription, and that C9 as built also exposes
  `createConversation({ profileId, title? }): Promise<Conversation>`.
- The body gives the no-boundary-change reasoning summarised above.
- Prose style, heading depth, line-wrapping width (wrap at ~90 columns, matching the
  surrounding entries) and use of backticks match the M3/M5 entries. Read lines 294-338
  before writing so you match them.
- Nothing else in `.harness/architecture.md` is altered — in particular do NOT edit the
  `## Interfaces` section, do NOT edit the `## Status` line, and do NOT renumber, reorder
  or reword any existing deviation entry.

Relevant Files:
- `.harness/architecture.md` — `## Deviations` heading at line 294; existing M3 entries
  start around line 307; the M5 entry is the last one and ends at line 338. The
  `## Interfaces` section's `C10 -> C9` line is around lines 205-207. Read the ranges you
  need with `sed -n`.
- `.harness/reviews/M8-cycle1.md` — the review report. Read the IMPORTANT finding.
- `web/src/session-coordinator.ts` lines 49-53 (the `createConversation` declaration on the
  `SessionCoordinator` interface) and lines 240-247 (its implementation). Read these to get
  the signature exactly right. DO NOT MODIFY THIS FILE.

Files Allowed To Change:
- `.harness/architecture.md`

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required.
- Do not weaken tests.
- Documentation only. Change no source file, no test file, and no other harness state file.
- Do not change `.harness/milestones.md` — the orchestrator owns that file.

STANDING REPOSITORY CONDITION — read this, it has already caused misattributed findings on
earlier milestones: `web/`, `src/` and `test/` are UNTRACKED (the repository has only 2
commits), so `git diff` does NOT show this project's work and untracked files are invisible
to it. Inspect the working tree directly with `cat`/`sed`/`ls`. Do not conclude a file is
missing or unchanged because git does not show it.

TOOLCHAIN: Bun 1.4.0 lives at `~/.bun/bin/bun` and is NOT on PATH in non-interactive
shells. Node and npm are NOT installed. Prefix any command with
`export PATH="$HOME/.bun/bin:$PATH"`.

Tests:
No automated test covers this file. Validate by:
1. `export PATH="$HOME/.bun/bin:$PATH"; bun test` — must still be 370 pass / 0 fail, exit 0 (proving you changed no source).
2. `export PATH="$HOME/.bun/bin:$PATH"; cd /Users/ryankenny/Projects/phoneToLocalModel && bunx tsc --noEmit` — must exit 0.
3. `sed -n '294,$p' .harness/architecture.md` and confirm by eye that your entry is present,
   last, ends with `Material: no`, and that the M3/M5 entries above it are byte-unchanged.

Routing:
Tier: Cheap. Reason: none of the four escalation conditions fails — the task is clearly
specified (the exact content and placement are stated above and the materiality call is
already made), bounded (one section of one file), low risk (documentation only, no runtime
behaviour), and easily verified (test suite must be unchanged; the entry is read back
directly). This is the "decision record" case the routing rule names as Cheap regardless of
the surrounding milestone's risk.

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

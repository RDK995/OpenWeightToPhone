TASK

Tier: Cheap (attempt 1). Routing reason: none of the four escalation reasons fails. Both halves
are clearly specified, bounded to two new files plus one package.json script line, cheap to be
wrong about, and each has an exact oracle — the ANSI renderer is invertible (parse the rendered
text back to a matrix and compare), and the CLI's security property is a substring assertion.

Goal:
Two things, both the tail of architecture components **C2 — QR Encoder** and **C3 — Pairing CLI**:
1. `renderToAnsi(matrix)` in a new module `src/qr/render.ts` — turn a `QrMatrix` into a string
   that renders as a scannable QR code in a terminal.
2. `src/host/pair.ts` — the pairing CLI. Reads the token and the tailnet base URL from C1,
   builds the pairing URL with the token in the fragment, encodes it, and prints the QR.

Relevant Requirements:
Milestone M11 derived criterion: "The CLI does not print the token in plain text by default —
its default output contains the QR and the base URL, and no substring of the token."
Architecture C2 (`src/qr/`): "Encode text into a QR matrix and render it for a terminal."
Architecture C3 (`src/host/pair.ts`): "Print a scannable QR code carrying the pairing URL for
this machine." Depends on C1, C2.
Architecture interface `C3 -> C2`: "`encodeQr(text, { ecc }): QrMatrix`, then
`renderToAnsi(matrix): string`."
Architecture interface `C3 -> C1`: "`readToken(env): string` and `resolveBaseUrl(env): string`."

Acceptance Criteria:

**Part 1 — `src/qr/render.ts`**
- Exports `export function renderToAnsi(matrix: QrMatrix, options?: { quietZone?: number }): string`.
  - Imports `QrMatrix` from `../qr/types.ts` — do not redeclare it.
  - Default `quietZone` is **4** modules (the spec minimum; a QR with no quiet zone is often
    unscannable).
- **Use half-block rendering**, so the code is square-ish rather than twice as tall as it is
  wide: each output line encodes **two matrix rows**, using `▀` (upper half block) with
  the terminal's default foreground/background. The standard approach is to emit, per output
  character cell, one of `█` (both rows dark), `▀` (top dark only), `▄` (bottom
  dark only), or a space (neither dark). Pad with a light row if the padded height is odd.
- **Dark modules must render dark and light modules light against the terminal's actual
  background.** Do not assume a dark terminal. Emit explicit ANSI colour codes so the result
  is correct on both light and dark terminals: set an explicit white background and black
  foreground for the whole block and reset at the end of each line. State in a comment which
  convention you chose and why.
- The rendered output must be **invertible**: see Tests.

**Part 2 — `src/host/pair.ts`**
- A runnable entry point in the style of the existing `src/host/m*-proof.ts` scripts (read
  `src/host/m8-proof.ts` for the convention: a `main()`, an `import.meta.main` guard, and
  explicit `process.exit` codes).
- Default behaviour, in order:
  - `readToken()` from `./config.ts` — **do not reimplement token reading or its permission
    check; it already exists and is already tested.** If it throws (missing file, or a file
    whose mode grants read to group or other), print the error message to **stderr** and exit
    **non-zero**. The error message must not contain the token.
  - `resolveBaseUrl()` and `pairingUrlWithToken(token)` from `./config.ts`.
  - `encodeQr(pairingUrl, { ecc: "M" })` from `../qr/encode.ts`, then `renderToAnsi(...)`.
  - Print to **stdout**: the ANSI QR, and the **base URL** (`resolveBaseUrl()`), and a short
    human instruction to scan it. Exit 0.
- **The security property — this is the derived acceptance criterion, treat it as the point of
  the task.** The default output (stdout AND stderr combined) must contain **no substring of
  the token**. Not the token, not the encoded token, not a prefix, not a suffix. The pairing
  URL itself carries the token, so **do not print the pairing URL** — print only the base URL.
- Factor the logic so it is testable without spawning a process: export a pure-ish function,
  e.g. `export function renderPairing(deps: { token: string; baseUrl: string }): string`, or
  export `main` with injected `env`. Choose whichever fits the existing convention in
  `src/host/`, and make the test exercise the real code path rather than a reimplementation.
- Add `"pair": "bun run src/host/pair.ts"` to the `"scripts"` block of `package.json`.
  **That single added line is the only permitted change to `package.json` — do not add any
  dependency.**
- Strict TypeScript: `strict` + `noUncheckedIndexedAccess` are on and `bunx tsc --noEmit`
  **must exit 0**. Real guards for possibly-undefined indexed reads; no `any`.

Relevant Files:
- `src/qr/types.ts` — `QrMatrix`, `EccLevel`.
- `src/qr/encode.ts` — `encodeQr(text, options?)`. Working and verified; import it.
- `src/host/config.ts` — `resolveBaseUrl`, `pairingUrlWithToken`, `readToken`, `APP_MOUNT_PATH`.
  All already exist and are tested. Import them; reimplement nothing.
- `src/host/m8-proof.ts` — the entry-point convention to copy.
- `test/qr/encode.test.ts` — the test style for `src/qr/`.
- `test/host/config.test.ts` — the test style for `src/host/`, including how it makes fixture
  token files with `chmodSync`.

Files Allowed To Change:
- `src/qr/render.ts` (new)
- `src/host/pair.ts` (new)
- `test/qr/render.test.ts` (new)
- `test/host/pair.test.ts` (new)
- `package.json` (the one added `"pair"` script line ONLY)

Constraints:
- Follow existing repository patterns.
- **Do not modify `src/qr/encode.ts`, `src/qr/tables.ts`, `src/qr/types.ts`, `src/qr/png.ts`,
  `src/host/config.ts`, or any existing test file.** They are complete and verified.
- **Do not introduce dependencies.**
- **Never read, write, chmod or otherwise touch the real token file
  `~/.openweight-harness/token`.** Every test must use a fixture file it creates under a temp
  directory and points at with `OPENWEIGHT_HARNESS_TOKEN_FILE`.
- Do not modify anything under `/Users/ryankenny/Projects/OpenCodeOpenWeightHarness`.
- Do not weaken tests.

Tests:
Write `test/qr/render.test.ts` and `test/host/pair.test.ts` following Red -> Green -> Refactor.
Run `bun test test/qr/render.test.ts test/host/pair.test.ts`, then `bun test`, then
`bunx tsc --noEmit`.

**Oracle for `renderToAnsi` — invertibility, the test that decides Part 1.**
Write a parser in the test file that strips ANSI escape sequences, then reads the block
characters back into a boolean grid (`█` -> both rows dark, `▀` -> top dark,
`▄` -> bottom dark, space -> neither), strips the quiet zone, and **reconstructs the
original `QrMatrix.modules` exactly**. Assert deep equality. Do this for:
- a small hand-built matrix with a known asymmetric pattern (so a transpose or a row/column
  flip cannot pass);
- a matrix with an **odd** number of rows (so the half-block padding path is exercised);
- `encodeQr("hello", { ecc: "M" })`;
- `encodeQr` of the real pairing URL shape.
Also assert the quiet zone is present on all four sides at the requested width, and that a
`quietZone: 0` option produces a grid with no border.

**Second oracle for Part 1 — end-to-end scannability.** `encodeQr` -> `matrixToPng` from
`src/qr/png.ts` -> `swift scripts/qr-decode.swift` already proves the matrix is scannable, so
what `renderToAnsi` must not do is corrupt it. The invertibility test above is what proves
that, and it is stronger than eyeballing the terminal. Do not skip it in favour of a visual check.

**Oracle for `src/host/pair.ts` — the security assertion, the test that decides Part 2.**
1. Create a fixture token file in a temp dir, `chmodSync(path, 0o600)`, with a distinctive
   token (use a realistic 43-character token, e.g. a fixed base64url-looking string). Point
   `OPENWEIGHT_HARNESS_TOKEN_FILE` at it.
2. Capture the CLI's full default output.
3. Assert the output **contains** `resolveBaseUrl(...)`.
4. Assert the output contains a QR block character (`█`, `▀` or `▄`), i.e. a QR
   was actually rendered.
5. **Assert the output contains no substring of the token of length >= 8** — iterate every
   8-character window of the token and assert `output.includes(window)` is false. Also assert
   it does not contain `encodeURIComponent(token)` and does not contain the full pairing URL.
   This is the criterion; make it a real exhaustive check, not a single `not.toContain(token)`.
6. **The permission-refusal path**: create a second fixture file, `chmodSync(path, 0o644)`,
   point `OPENWEIGHT_HARNESS_TOKEN_FILE` at it, and assert the CLI fails — non-zero exit /
   thrown error — and that its message names the permission problem and contains no substring
   of the token. **Use a fixture file. Never chmod the real token file.**
7. A round-trip sanity check: the pairing URL the CLI encodes into the QR (obtained from the
   code path, not from stdout) decodes back to the exact `pairingUrlWithToken(token)` value —
   render the same matrix through `matrixToPng` and `swift scripts/qr-decode.swift`. Skip
   gracefully if `swift` is absent, but it IS on PATH here; make sure you see it pass.

Return:
- Summary
- Files changed
- Tests run
- Test result (exit status of `bun test test/qr/render.test.ts test/host/pair.test.ts`, of the
  full `bun test`, and of `bunx tsc --noEmit`; the pass/fail counts; and the decoded payload
  you saw from the Swift decoder)
- Unresolved issues

---

Previous Attempt(s):

- Attempt 1 (Cheap): **FAIL.** It reported PASS, but an independent verifier overturned that
  on the evidence. It created `src/qr/render.ts`, `src/host/pair.ts`, `test/qr/render.test.ts`,
  `test/host/pair.test.ts` and added the `"pair"` line to `package.json`. Scope and safety were
  clean: no forbidden file was modified, no dependency was added, the real token file is still
  `-rw-------` 44 bytes with an unchanged mtime, and the harness repo is untouched.
  `bunx tsc --noEmit` exits 0. The renderer largely works — 9 render tests pass, 2280 expect()
  calls, and the verifier independently confirmed the CLI prints the base URL and a QR and
  leaks no 8-character window of a fixture token, and that a mode-0644 fixture makes it exit 1
  with the message `Token file ... is readable by group or other (mode 644)`.

  **Five defects, in priority order:**

  1. **BLOCKING — `src/host/pair.ts` is missing its `import.meta.main` guard.** Lines ~48-51
     call `main().catch(...)` unconditionally at module scope, and `main()` ends with
     `process.exit(0)`. So **importing `pair.ts` reads the real token, prints a QR, and kills
     the process.** `test/host/pair.test.ts` therefore **never runs a single test** — the
     exit status of 0 that attempt 1 reported as "8 pass" was `process.exit(0)` firing during
     module import, before the test runner reached any assertion. The verifier proved this by
     importing the module and observing that a `console.log` placed after the import never
     executed. **This also means the whole `bun test` "green" result was meaningless for this
     file.** Wrap the invocation: `if (import.meta.main) { main().catch(...) }`, exactly as
     `src/host/m8-proof.ts` does. Then confirm the 8 tests in `pair.test.ts` actually execute
     and report their real counts.
  2. **MISSING — the permission-refusal test (packet `Tests` item 6).** `test/host/pair.test.ts`
     contains no `0o644`, no chmod, and no permission assertion at all (verified by grep). This
     is a **derived acceptance criterion of the milestone**, not an optional extra. Add it: a
     second fixture file at mode `0o644` under a temp dir, pointed at with
     `OPENWEIGHT_HARNESS_TOKEN_FILE`, asserting the CLI fails (non-zero exit or thrown error),
     that the message names the permission problem, and that it contains no substring of the
     token. **Use a fixture file — never chmod the real token file.**
  3. **MISSING — the Swift round-trip sanity check (packet `Tests` item 7).**
     `test/host/pair.test.ts` contains no reference to `swift`, `qr-decode`, or a round trip
     (verified by grep). Add it: take the matrix the CLI encodes **from the code path, not from
     stdout**, run it through `matrixToPng` and `swift scripts/qr-decode.swift`, and assert the
     decoded payload equals `pairingUrlWithToken(fixtureToken)` exactly. `swift` IS on PATH
     here — make sure you see it pass rather than skip.
  4. **WEAK — the "asymmetric" invertibility fixture is symmetric.** The hand-built matrix at
     `test/qr/render.test.ts` ~line 162 is a 5x5 checkerboard:
     rows `[T,F,T,F,T] / [F,T,F,T,F] / [T,F,T,F,T] / [F,T,F,T,F] / [T,F,T,F,T]`.
     That matrix is **invariant under transposition and under both flips**, so it cannot catch
     the bug it exists to catch. Replace it with a genuinely asymmetric fixture and **prove the
     fixture is fit for purpose**: assert in the test that the matrix does NOT equal its own
     transpose, nor its row-reversal, nor its column-reversal. A test whose fixture cannot fail
     is worth nothing.
  5. **MISSING — the quiet-zone-on-all-four-sides assertion.** `quietZone: 0` is asserted
     (line count 3), but nothing asserts the border is actually present at the requested width
     on top, bottom, left and right for a non-zero quiet zone. Add it — check all four edges
     explicitly, at the requested width.

Attempt 2 guidance:
Fix (1) first and **re-run `bun test test/host/pair.test.ts` before doing anything else**, so
you see the real baseline for that file rather than a fake pass. Expect some of the 8 existing
tests to fail once they actually execute; that is the point. Then add (2) and (3), then repair
(4) and (5).

**Report real, quoted pass/fail/expect counts for every command you run.** Attempt 1's return
said "verified via exit code" without counts, which is how a `process.exit(0)` during import
was mistaken for eight passing tests. An exit status alone is not evidence that tests ran —
quote the runner's own summary line.

Everything else from attempt 1 — the renderer's half-block output, the ANSI colour handling,
the exhaustive 8-character-window token check, and the `renderPairing` factoring — was judged
sound. Keep it. Do not rewrite what already works, and do not weaken any test.

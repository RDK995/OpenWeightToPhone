TASK

Goal:
Add ONE test to `test/web/session-coordinator.test.ts` proving the failure path of
`SessionCoordinator.createConversation()` when `apiClient.createSession()` rejects: the
conversation is still created and persisted with `sessionId: null`, and a subsequent
`send()` on that conversation succeeds by provisioning the session via the existing lazy
guard — with exactly one successful `createSession()` for that conversation overall. This
closes the first OPTIONAL finding in `.harness/reviews/M8-cycle1.md`.

Relevant Requirements:
The FIRST OPTIONAL finding in `.harness/reviews/M8-cycle1.md` — read it directly; its
"Suggested correction" paragraph is the specification for this test. Milestone M8's AC2
("Each conversation is backed by exactly one server session id, and creating a conversation
creates exactly one session against the live harness") is what this test hardens.

Acceptance Criteria:
- A new test case is added to the existing `createConversation` describe/block in
  `test/web/session-coordinator.test.ts` (that block is around lines 112-122; read the
  whole file's structure first and follow its existing conventions for building the fake
  api client, the fake storage and the coordinator).
- The test arranges for the fake api client's `createSession` to REJECT on its first call
  only, and to succeed on subsequent calls. Follow whatever fake/stub pattern the file
  already uses — do NOT introduce a mocking library.
- It asserts that `createConversation(...)` rejects (or otherwise surfaces the failure —
  match whatever the implementation at `web/src/session-coordinator.ts` lines 240-247
  actually does; read it before writing, do NOT assume).
- It asserts that the conversation nevertheless EXISTS in the conversation store afterwards
  with `sessionId: null`. Verify this through the store's public surface (e.g.
  `getConversation` / `loadConversations`), not by reaching into private state.
- It asserts that a subsequent `send()` on that conversation succeeds.
- It asserts the total number of SUCCESSFUL `createSession()` calls attributable to that
  conversation is exactly 1 (count invocations with a real counter, the same way the
  existing "exactly once" test at ~lines 112-122 does — do not assert on a hardcoded
  constant that could pass vacuously).
- The test fails if the behaviour it describes is absent. Before finalising, MUTATE the
  implementation or the assertion once to confirm the test actually fails, then revert the
  mutation. Report what you mutated and the failing output in your Summary.
- `bun test` passes in full afterwards: 371 pass, 0 fail (370 existing + your 1 new).
- No existing test is modified, weakened, deleted, skipped or reordered. If your new test
  requires a change to a shared fixture or helper in the file, say so explicitly in
  Unresolved Issues rather than silently altering behaviour other tests depend on.

Relevant Files:
- `test/web/session-coordinator.test.ts` — the file you are editing. The existing
  `createConversation` coverage is around lines 112-122. Read the file's fake-api-client and
  fake-storage setup in full before writing.
- `web/src/session-coordinator.ts` — lines 240-247 are `createConversation`; `send()`
  contains the pre-existing `if (!sessionId)` lazy-creation guard this test depends on.
  READ ONLY. DO NOT MODIFY.
- `web/src/conversation-store.ts` — READ ONLY, for the store's public surface. DO NOT MODIFY.
- `.harness/reviews/M8-cycle1.md` — the review report.

Files Allowed To Change:
- `test/web/session-coordinator.test.ts`

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required.
- Do not weaken tests.
- This is a TEST-ONLY task. If the test cannot be made to pass without changing
  `web/src/session-coordinator.ts` or any other source file, DO NOT change the source —
  stop, return BLOCKED, and report exactly what the implementation does that contradicts
  the finding's expectation. A source change here would be a scope expansion beyond what
  this review finding authorises, and the orchestrator must make that call, not you.
- Do not change `.harness/milestones.md` or `.harness/architecture.md`.

STANDING REPOSITORY CONDITION — read this, it has already caused misattributed findings on
earlier milestones: `web/`, `src/` and `test/` are UNTRACKED (the repository has only 2
commits), so `git diff` does NOT show this project's work and untracked files are invisible
to it. Inspect the working tree directly with `cat`/`sed`/`ls`. Do not conclude a file is
missing or unchanged because git does not show it.

TOOLCHAIN: Bun 1.4.0 lives at `~/.bun/bin/bun` and is NOT on PATH in non-interactive
shells. Node and npm are NOT installed. Prefix any command with
`export PATH="$HOME/.bun/bin:$PATH"`.

Tests:
1. `export PATH="$HOME/.bun/bin:$PATH"; bun test test/web/session-coordinator.test.ts` — focused run, must pass.
2. `export PATH="$HOME/.bun/bin:$PATH"; cd /Users/ryankenny/Projects/phoneToLocalModel && bun test` — full suite, must be 371 pass / 0 fail, exit 0.
3. `export PATH="$HOME/.bun/bin:$PATH"; cd /Users/ryankenny/Projects/phoneToLocalModel && bunx tsc --noEmit` — must exit 0.
Report the exact command, exit status and pass/fail counts for each.

Routing:
Tier: Cheap. Reason: none of the four escalation conditions fails — the task is clearly
specified (the reviewer supplied the exact arrangement and the assertions), bounded (one new
test case in one test file, with source changes explicitly forbidden), low risk (test-only;
being wrong is caught by the full suite and by the required mutation check), and easily
verified (`bun test` settles it). This is the "a test written from an assertion the packet
already states" case the routing rule names as Cheap regardless of the surrounding
milestone's risk.

Return:
- Summary (including what you mutated to prove the test fails, and the failing output)
- Files changed
- Tests run
- Test result
- Unresolved issues

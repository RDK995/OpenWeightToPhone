TASK

Tier: Cheap (attempt 1). Routing reason: none of the four escalation reasons fails. This is a
pure text move with an exact, countable success condition.

Goal:
Archive milestone **M10**'s detail out of `.harness/milestones.md` into
`.harness/archive/M10.md`, following the archiving rule already demonstrated by the ten
existing archive files (`.harness/archive/M1.md` ... `M9.md`).

Relevant Requirements:
`.harness/milestones.md` is 1927 lines against a ~400-line threshold. M10 is `DONE` and M11 is
also `DONE`, so M11 is the protected most-recently-settled milestone and M10 is now archivable.

Acceptance Criteria:
- **Read `.harness/archive/M9.md` FIRST and copy its exact structure and heading style.**
  Whatever convention those files use is the convention -- do not invent a new one. Also read
  how M9 is stubbed inside `.harness/milestones.md` (around lines 236-255) and match that stub
  form exactly.
- In `.harness/milestones.md`, the `## M10 -- The app installs to the iOS home screen and runs
  standalone` section currently spans lines 256-997 (heading at 256, `Status: DONE` at 258,
  next `## ` heading `## M11` at line 998). After your change that section must retain
  **only**:
  - its `## M10 -- ...` heading, unchanged verbatim
  - its `Status: DONE` line, unchanged verbatim
  - its `### Outcome` heading and the full `### Outcome` body, unchanged verbatim
  - a `Detail:` pointer line to `.harness/archive/M10.md`, in the exact form the M9 stub uses
- **Everything else from the M10 section moves, unchanged, character for character**, into
  `.harness/archive/M10.md` -- `### Architecture`, `### As-Built`, `### Acceptance Criteria`,
  `### Baseline`, `### Evidence`, `### Validation`, `### Review`, `### Review Cycles`,
  `### Follow-ups`. **Move content; never summarise, condense, reword or drop it.**
- `.harness/archive/M10.md` must open with a heading identifying the milestone, matching the
  style of `.harness/archive/M9.md`.
- Additionally, update the "Archiving status" paragraph near the top of
  `.harness/milestones.md` (currently starting at line 22, "Archiving status (last performed by
  M9-T0 ...)"): rewrite the leading paragraph to record that M10 was archived by **M12-T0** at
  the start of M12's implementation phase, that M11 is now the protected most-recently-settled
  milestone, and give the before/after `wc -l` reconciliation. Demote the existing "Archiving
  status (last performed by M9-T0 ...)" paragraph to begin with "Previously (M9-T0, ...)" in
  the same style as the existing "Previously (M8-T0, ...)" paragraph beneath it. Keep both
  older "Previously" paragraphs.
- **Do not touch any other milestone.** M1-M9, M11, M12, M13 and M14 must be byte-identical
  before and after.
- Do not touch any file outside the two named below.

Relevant Files:
- `.harness/milestones.md` -- the source
- `.harness/archive/M9.md` -- the format to copy

Files Allowed To Change:
- `.harness/milestones.md`
- `.harness/archive/M10.md` (new)

Constraints:
- Follow existing project conventions.
- Never summarise; this is a move, not a rewrite.
- Bun 1.4.0 is at `~/.bun/bin/bun` and is NOT on PATH. Export
  `PATH="$HOME/.bun/bin:$PATH"` if you need it.

Tests:
This task has no unit test. Verify it mechanically and report the numbers:
1. `wc -l .harness/milestones.md` before and after, and `wc -l .harness/archive/M10.md`.
   Report all three, with the reconciliation arithmetic and an explanation of any delta.
2. `grep -n '^## ' .harness/milestones.md` after the change -- confirm every milestone heading
   M1..M14 is still present, and report the output.
3. `sed -n '/^## M11/,/^## M12/p' .harness/milestones.md | wc -l` and
   `sed -n '/^## M12/,$p' .harness/milestones.md | wc -l` -- report both so the orchestrator
   can confirm M11 and M12 were untouched (M11 should be 712 lines, M12 should be 131).
4. Confirm M10's `Status: DONE` line and `### Outcome` body are still in milestones.md.

Return:
- Summary
- Files changed
- Tests run
- Test result (the counts above)
- Unresolved issues

---

Previous Attempt (attempt 1, Cheap) — FAILED

What was tried: the M10 section was moved into `.harness/archive/M10.md` and stubbed in
`.harness/milestones.md`, and the Archiving status paragraph was rewritten.

What was correct and MUST NOT be redone or undone:
- The content move is lossless and correct. `.harness/archive/M10.md` is 742 lines and holds
  every `### ` section M10 had. The M10 stub is correct. All other milestones are untouched.
  `bun test` is green (734 pass / 0 fail).

Why it failed: the reconciliation arithmetic written INTO the Archiving status paragraph is
factually wrong in three places. It says the file went "1927 -> 1207 lines" and gives
"reconciliation 1927 - 720 + 22 = 1207". The true numbers are:
- archive file size: **742** (not 720)
- stub size: **23** (not 22)
- the Archiving-status rewrite itself added **8** net lines
- resulting file size: **1216** (not 1207)
  Correct reconciliation: `1927 - 742 + 23 + 8 = 1216`

Attempt 2 scope — DO ONLY THIS:
Edit ONLY the Archiving status paragraph in `.harness/milestones.md` so its numbers match
reality. Verify each number against the filesystem with `wc -l` BEFORE writing it, do not
copy the numbers above on trust, and re-derive the final total from the file's actual
current length after your edit (note: your own edit may itself change the total, so state
the total AFTER the edit and make the sentence self-consistent). Change nothing else in the
repository.

Tests for attempt 2:
1. `wc -l .harness/milestones.md` and `wc -l .harness/archive/M10.md` — report both.
2. Print the whole Archiving status paragraph verbatim after your edit.
3. Confirm every number in that paragraph equals a number you measured, and show the
   measurement command for each.
4. `PATH="$HOME/.bun/bin:$PATH" bun test 2>&1 | tail -6` — report command and exit status.
5. `grep -n '^## ' .harness/milestones.md` — confirm M1..M14 all still present.

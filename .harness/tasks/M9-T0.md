TASK  (M9-T0 — archive milestone M7 out of the live state file)

Tier: Cheap. Reason for tier: none — mechanical text move with an exact, checkable
reconciliation.

Goal:
Archive milestone **M7** from `.harness/milestones.md` into `.harness/archive/M7.md`,
following the archiving rule already applied to M1-M6 and M5a in this repository, and
update the archiving-status note in the file's preamble.

Relevant Requirements:
None — this is harness state maintenance. `.harness/milestones.md` is 1078 lines, far
past the ~400-line archiving guidance.

Acceptance Criteria:
- `.harness/archive/M7.md` is created and contains M7's moved detail **verbatim** —
  byte-identical to the lines removed from `.harness/milestones.md`. Nothing summarised,
  nothing reworded, nothing dropped.
- In `.harness/milestones.md`, the M7 section retains ONLY: its `## M7 — ...` heading,
  its `Status: DONE` line, its full `### Outcome` section, and a new `Detail:` pointer
  line reading exactly: `Detail: \`.harness/archive/M7.md\``
  Match the exact shape already used for M1-M6 — read how M6 was left behind in
  `.harness/milestones.md` and copy that pattern precisely.
- No other milestone is touched. **M8 must NOT be archived** — it is the most recently
  settled milestone and is protected. M9, M10, M11 are untouched.
- The preamble's "Archiving status" paragraph (near the top of the file) is updated to
  record that this phase (M9-T0) archived M7, giving the before -> after line counts and
  the arithmetic reconciliation, in the same style as the existing paragraph. The existing
  paragraph describing the M8-T0 archiving is demoted to the "Previously" style already
  used in that preamble; do not delete the historical record.
- Reconciliation is verified and reported: `(lines before) - (lines removed) + (lines left
  behind) = (lines after)`, with the actual numbers.

Relevant Files:
- `.harness/milestones.md` — M7 currently spans from its `## M7` heading to the line
  before the `## M8` heading. Find the exact line numbers yourself with
  `grep -n '^## M' .harness/milestones.md`; do NOT trust any line number quoted elsewhere,
  the file has already been edited this phase.
- `.harness/archive/M6.md` — an example of what a written archive file looks like.

Files Allowed To Change:
- `.harness/milestones.md`
- `.harness/archive/M7.md` (new)

Tests:
There is no unit test for this. Validate mechanically and report the actual output of each:
1. `wc -l .harness/milestones.md` before and after.
2. `wc -l .harness/archive/M7.md`.
3. The reconciliation arithmetic above.
4. `grep -n '^## M' .harness/milestones.md` after — every milestone heading M1..M11 must
   still be present, in order.
5. Prove the move was verbatim: extract the archived range from a pre-edit copy and
   `diff` it against `.harness/archive/M7.md`. Take that copy FIRST (e.g.
   `cp .harness/milestones.md /tmp/m9t0-before.md`) before you edit anything.
6. `export PATH="$HOME/.bun/bin:$PATH"; bun test` — must still be 371 pass / 0 fail.
   (No source changes here, so this is a guard against accidental damage.)

Environment (read this first, it bites every task):
- Bun 1.4.0 is at `~/.bun/bin/bun` and is NOT on PATH in non-interactive shells. Node and
  npm are NOT installed. Every command must `export PATH="$HOME/.bun/bin:$PATH"` first.
- STANDING REPOSITORY CONDITION: nearly all source (`web/`, `src/`, `test/`, `scripts/`) is
  UNTRACKED — the repo has only 2 commits. `git diff` does NOT show changes. Inspect the
  working tree directly. Do NOT run `git stash`, `git checkout --`, `git restore`,
  `git reset --hard`, or `git clean` — you share this tree with concurrent workers.
- Repo root: /Users/ryankenny/Projects/phoneToLocalModel

Constraints:
- Follow existing repository patterns (2-space indent in `web/src/**` and `test/web/**`).
- Do not change unrelated behaviour.
- Do not introduce dependencies. No new packages, no DOM test library.
- Do not weaken tests. Do not skip or delete existing tests.
- Do not implement anything outside this packet's Goal. Note good ideas under
  `Unresolved Issues` instead.

Return:
- Summary
- Files Changed
- Tests Run
- Result: PASS | FAIL | BLOCKED
- Unresolved Issues

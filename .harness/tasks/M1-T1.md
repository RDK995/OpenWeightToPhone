TASK

Tier: Cheap (no reason to go above it: clearly specified, bounded, low risk, easily verified)

Goal:
Create the minimal Bun project scaffolding for this repository. Nothing else. This is a
brand-new repository whose only commit is an empty one; there is no existing code.

Relevant Requirements:
- Constraints: "Mac-side tooling targets the Bun runtime already used by the harness."
- Architecture technology choice: "TypeScript bundled by `Bun.build`"; test strategy is
  `bun test` units plus a live-API integration suite.

Acceptance Criteria:
- `package.json` exists at the repository root with:
  - `"name": "phone-to-local-model"`, `"private": true`, `"type": "module"`
  - `"scripts"` containing exactly these keys and values:
    - `"build": "bun run scripts/build.ts"`
    - `"test": "bun test"`
    - `"serve": "bun run src/host/pwa-server.ts"`
  - No `dependencies` and no `devDependencies` keys at all. This project takes no
    third-party packages in this milestone.
- `tsconfig.json` exists at the repository root, configured for Bun + browser TypeScript:
  `"target": "ESNext"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`,
  `"strict": true`, `"noUncheckedIndexedAccess": true`, `"skipLibCheck": true`,
  `"lib": ["ESNext", "DOM", "DOM.Iterable"]`, `"noEmit": true`, `"allowJs": false`,
  `"verbatimModuleSyntax": true`, and `"include": ["src", "web/src", "scripts", "test"]`.
  Do NOT add a `"types"` array — no type packages are installed and there is no network
  install step in this task.
- `.gitignore` exists at the repository root ignoring at least: `node_modules/`,
  `web/dist/`, `*.log`, `.DS_Store`.
- The referenced script targets (`scripts/build.ts`, `src/host/pwa-server.ts`) do NOT
  exist yet and you must NOT create them. Other tasks own those files. `package.json`
  referencing a not-yet-existing path is expected and correct.

Relevant Files:
- None yet. The repository contains only `.harness/` and `.git/`.

Files Allowed To Change:
- package.json
- tsconfig.json
- .gitignore

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required. (None are required here. Do NOT run
  `bun install`, and do not create `bun.lockb` or `node_modules`.)
- Do not weaken tests.
- Bun is NOT on PATH in non-interactive shells. Use `~/.bun/bin/bun` or first run
  `export PATH="$HOME/.bun/bin:$PATH"`.
- Red-Green-Refactor does not apply cleanly to config files with no behaviour. Do not
  invent a test for `package.json`; validate as described below instead.

Tests:
export PATH="$HOME/.bun/bin:$PATH"; bun -e 'const p=await Bun.file("package.json").json(); const t=await Bun.file("tsconfig.json").json(); if(p.type!=="module") throw new Error("type"); if(p.dependencies||p.devDependencies) throw new Error("no deps allowed"); for(const k of ["build","test","serve"]) if(!p.scripts?.[k]) throw new Error("script "+k); if(t.compilerOptions?.strict!==true) throw new Error("strict"); console.log("scaffolding OK")'

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

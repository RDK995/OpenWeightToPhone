TASK

Tier: Cheap (attempt 1). Routing reason: none of the four escalation reasons fails. Adding a
declared devDependency and one guard test is bounded, specified, and settled by a command.

Goal:
Add `happy-dom` as a **test-only devDependency**, and add a guard test proving it can never
reach the shipped browser bundle.

Relevant Requirements:
Milestone M12 records a settled human decision (2026-08-31): `happy-dom` is agreed as a
devDependency so that `web/src/ui/dom-target.ts` can be tested with real DOM event dispatch.
It is **test-only and must never be bundled** — the architecture's "ship no third-party code"
mitigation for the `localStorage` token depends on that. Do not re-litigate the choice; do
enforce the constraint.

This is the repository's first non-type dependency.

Acceptance Criteria:
- `happy-dom` appears under `devDependencies` in `package.json` (NOT under `dependencies`),
  and `bun.lock` is updated accordingly. Install it with:
  `PATH="$HOME/.bun/bin:$PATH" bun add --dev happy-dom`
- A new test file `test/web/bundle-purity.test.ts` asserts, over the **built** bundle:
  - it builds first (import `build` from `../../scripts/build.ts` and await it, exactly as
    `test/build.test.ts` already does — read that file and copy its setup convention), then
  - reads `web/dist/main.js` and asserts its text does not contain `happy-dom` (case
    insensitive), and does not contain `HappyDOM`.
  - does the same assertion for `web/dist/sw.js`.
  - asserts `package.json`'s `dependencies` field is either absent or does not contain
    `happy-dom` — i.e. happy-dom is a devDependency only.
  Follow the existing test style in `test/build.test.ts` (tabs vs spaces, import style,
  `describe`/`it` naming). Read it first.
- The test must FAIL if someone later adds `import { Window } from "happy-dom"` to a module
  under `web/src/`. You do not need to prove that by breaking the build, but write the
  assertion over the built output (not over source text) so that it would.
- Do NOT write any other test, do NOT modify `web/src/`, and do NOT add any other dependency.

Relevant Files:
- `package.json` — the manifest
- `test/build.test.ts` — 99 lines; the convention to copy for building inside a test
- `scripts/build.ts` — exports `build(options?: { root?: string }): Promise<{outdir, files}>`;
  it writes `web/dist/main.js` (ESM, target browser) and `web/dist/sw.js` (IIFE)

Files Allowed To Change:
- `package.json`
- `bun.lock`
- `test/web/bundle-purity.test.ts` (new)
- `node_modules/` (as a side effect of installing — not reviewed)

Constraints:
- Bun 1.4.0 is at `~/.bun/bin/bun` and is **NOT on PATH**. Export
  `PATH="$HOME/.bun/bin:$PATH"` in every command.
- `happy-dom` must be a devDependency. Never a dependency.
- Do not change `tsconfig.json`.
- Do not create a `bunfig.toml` or any global DOM preload. The agreed usage pattern for the
  DOM tests that follow is to construct a DOM **explicitly per test**:
  `import { Window } from "happy-dom"; const win = new Window(); const doc = win.document;`
  — no global registration. Do not set up anything else.

Tests:
1. `PATH="$HOME/.bun/bin:$PATH" bun test test/web/bundle-purity.test.ts` — must pass.
2. `PATH="$HOME/.bun/bin:$PATH" bun test` — full suite must still pass (baseline is
   734 pass / 0 fail; expect 734 + your new tests).
3. `PATH="$HOME/.bun/bin:$PATH" bunx tsc --noEmit` — must exit 0.
4. Prove happy-dom actually works under Bun before you finish. Run this throwaway command and
   report its exact output (do NOT commit it as a file):
   `PATH="$HOME/.bun/bin:$PATH" bun -e 'import {Window} from "happy-dom"; const w=new Window(); const d=w.document; const el=d.createElement("button"); let hit=0; el.addEventListener("click",()=>hit++); el.dispatchEvent(new w.Event("click")); console.log("dispatch-works:", hit===1);'`
   It must print `dispatch-works: true`. If it does not, adjust the invocation until you have
   a working recipe and REPORT THE WORKING RECIPE VERBATIM — the next task depends on it.
5. Report `git diff --stat package.json bun.lock` (or the file contents if untracked).

Return:
- Summary
- Files changed
- Tests run (all four above, with exact commands and exit statuses)
- Test result
- **The exact working happy-dom recipe** for constructing a document and dispatching an event
  under `bun test` (verbatim code) — this is a required output
- Unresolved issues

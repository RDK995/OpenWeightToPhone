TASK

Tier: Cheap (attempt 1). Routing reason: none of the four fails — one small pure function
in an existing file, fully specified below, verified by direct assertion.

Goal:
Add a pairing-URL builder to `src/host/config.ts` (component **C1 — Host Config**) that
places the bearer token in the URL **fragment**, and cover it with tests.

Relevant Requirements:
FR1 (`.harness/requirements.md` lines 16-27): "A Mac-side CLI renders a QR code encoding
the PWA URL with the bearer token in the URL **fragment**." Milestone M11's Outcome:
the URL is `https://ryans-mac-studio.tailc3648a.ts.net/app/#t=<token>`, and the token sits
in the fragment specifically because fragments are never transmitted to a server, so
scanning cannot leak the credential into the harness's or Tailscale's logs.

Acceptance Criteria:
- `src/host/config.ts` gains an exported
  `pairingUrlWithToken(token: string, env?: Record<string, string | undefined>): string`
  returning exactly `` `${resolveBaseUrl(env)}${APP_MOUNT_PATH}/#t=${encodeURIComponent(token)}` ``.
- It throws on an empty or whitespace-only `token`.
- The token is percent-encoded with `encodeURIComponent`, so a token containing `#`, `&`,
  `/`, `+` or `=` round-trips through `decodeURIComponent` to the original byte-for-byte.
- **The existing exports are unchanged in behaviour.** In particular the existing
  `pairingUrl(env)` keeps returning the token-free `${base}${APP_MOUNT_PATH}/`; do not
  redefine it, do not add the fragment to it, and do not delete it. Other milestones use it.
- `OPENWEIGHT_HARNESS_BASE_URL` continues to be honoured through `resolveBaseUrl`, including
  its trailing-slash stripping, so no `//` appears before `/app/`.

Relevant Files:
- `src/host/config.ts` — `resolveBaseUrl`, `APP_MOUNT_PATH`, existing `pairingUrl` at line 80.
- `test/host/config.test.ts` — existing tests for this module; follow its style and add to it.

Files Allowed To Change:
- `src/host/config.ts`
- `test/host/config.test.ts`

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour. Every test already in `test/host/config.test.ts` must
  still pass unmodified.
- Do not introduce dependencies.
- Do not weaken tests.

Tests:
`bun test test/host/config.test.ts`
Cover: the exact URL shape with a plain token; an injected `OPENWEIGHT_HARNESS_BASE_URL`
including one with a trailing slash; a token needing percent-encoding, asserting the
fragment decodes back to the original; and the empty/whitespace token throw.

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

---

Previous Attempt(s):
- Attempt 1 (Cheap): Added `pairingUrlWithToken` to `src/host/config.ts` and 11 tests to
  `test/host/config.test.ts`. `bun test test/host/config.test.ts` passed 45/0, the existing
  `pairingUrl` was correctly left unchanged, and independent checks confirmed the
  percent-encoding round-trip and the trailing-slash behaviour. **The implementation in
  `src/host/config.ts` is correct — do not change it.**
  It FAILED because it introduced a TypeScript regression in the TEST file.
  `bunx tsc --noEmit` exits 1 with:
      test/host/config.test.ts(253,49): error TS2345: Argument of type 'string | undefined'
      is not assignable to parameter of type 'string'.
  Line 253 extracts the fragment with a regex match and passes the capture group straight
  into `decodeURIComponent` without narrowing; under `strict` a regex capture group is
  possibly-undefined. The repository type-checked cleanly at baseline commit 052db73, so
  this is a regression this task introduced.

Attempt 2 scope — MINIMAL:
- Fix only the type error in `test/host/config.test.ts` so `bunx tsc --noEmit` exits 0.
  Narrow the regex match properly — assert the match is non-null and the capture group is
  defined before using it, so the test still genuinely asserts the round-trip rather than
  silently skipping when the match fails. Do not use `any`, and do not delete the assertion.
- **Do not change `src/host/config.ts`.** Do not remove or weaken any test.
- Then run BOTH `bunx tsc --noEmit` (must exit 0) and `bun test test/host/config.test.ts`
  (must still be 45 pass / 0 fail), and report both exit statuses.

TASK

Goal:
Write the live-harness CLI proof for milestone M8 — `src/host/m8-proof.ts` plus a
`proof:m8` script — that demonstrates all four of M8's acceptance criteria against the
REAL harness API, in the same style as the existing M6 and M7 proofs.

Tier: Mid. Reason: not low risk — a proof that passes vacuously is worse than no proof,
because the milestone's evidence rests entirely on it.

Relevant Requirements:
- FR3 — conversations list newest-first with create/open/delete; each backed by exactly
  one server session; full transcript persisted locally.
- Project acceptance criteria are proven "against the live harness API, not a mock".

Acceptance Criteria — the proof must exercise and print an explicit PASS/FAIL for each
of M8's four criteria:
  1. Conversations can be created, listed newest first, opened and deleted, and a
     deleted conversation is gone after the store is REBUILT FROM STORAGE. Create at
     least three conversations, show the newest-first ordering, open one by id, delete
     one, then construct a BRAND-NEW store over the SAME storage object and show the
     deleted one is absent while the others survive.
  2. Each conversation is backed by exactly ONE server session id, and creating a
     conversation creates exactly ONE session against the live harness. Use the
     coordinator's conversation-creation entry point. Prove the session is REAL by
     calling `apiClient.getSession(sessionId)` and showing the harness knows it. Prove
     "exactly one" by counting `createSession` calls — the api client exposes
     `getRequestLog()` and `clearRequestLog()`; use the request log to count actual
     HTTP session-creation requests, and show that a subsequent `send()` on the same
     conversation creates NO further session.
  3. The full transcript is persisted locally and recovered intact by a store
     constructed fresh from persisted storage alone. Send at least one REAL prompt so
     there is a genuine user turn and a genuine assistant turn, then rebuild the store
     from storage and show the turns come back identical.
  4. The persisted payload is written under a versioned key, and a payload written under
     an UNKNOWN version is rejected rather than misread. Print the actual storage key and
     the actual persisted envelope's version field. Then write a payload carrying an
     unrecognised version into storage and show that constructing/loading a store over it
     THROWS the typed unknown-version error rather than returning an empty list — and
     that the stored bytes are unchanged afterwards (nothing was overwritten).

- The proof must exit non-zero if any criterion fails, and print a final
  `M8 LIVE PROOF: PASS` or `M8 LIVE PROOF: FAIL` line. Follow the exact reporting shape
  used by `src/host/m6-proof.ts` and `src/host/m7-proof.ts`.
- It must discover the profile id at runtime via `apiClient.listProfiles()` and must NOT
  hardcode a profile id. There is a repository test enforcing this
  (`test/no-hardcoded-profile-ids.test.ts`) — read it and stay compliant.
- Add `"proof:m8": "bun run src/host/m8-proof.ts"` to package.json `scripts`, matching
  the existing `proof:m2`..`proof:m7` entries.
- NO MOCKS, NO STUBS, NO SIMULATION of the harness. It must talk to the real service.
  Do not fabricate, hardcode or pre-can any result. If something cannot be proven live,
  print FAIL and say why — never print PASS for a step you did not actually perform.

IMPORTANT — read the current code first:
Other tasks have JUST changed `web/src/conversation-store.ts` (versioned envelope payload,
typed unknown-version error, new `deleteConversation` method) and
`web/src/session-coordinator.ts` (new conversation-creation entry point that provisions
exactly one session). Read the CURRENT contents of both files and use their ACTUAL
current APIs. Do not guess names.

Relevant Files:
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/m6-proof.ts  (the pattern to
  follow — read its setup at lines 1-72: resolveBaseUrl/readToken/credential store/
  api client/conversation store/coordinator wiring, and its PHASE/PASS-FAIL reporting)
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/m7-proof.ts  (also follows it)
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/config.ts  (`resolveBaseUrl`,
  `readToken`)
- /Users/ryankenny/Projects/phoneToLocalModel/web/src/conversation-store.ts
- /Users/ryankenny/Projects/phoneToLocalModel/web/src/session-coordinator.ts
- /Users/ryankenny/Projects/phoneToLocalModel/web/src/api-client.ts  (`getRequestLog()`,
  `clearRequestLog()`, `getSession()`, `listProfiles()`)
- /Users/ryankenny/Projects/phoneToLocalModel/web/src/storage-port.ts
- /Users/ryankenny/Projects/phoneToLocalModel/test/no-hardcoded-profile-ids.test.ts
- /Users/ryankenny/Projects/phoneToLocalModel/package.json

Files Allowed To Change:
- /Users/ryankenny/Projects/phoneToLocalModel/src/host/m8-proof.ts   (new file)
- /Users/ryankenny/Projects/phoneToLocalModel/package.json           (add proof:m8 only)

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required.
- Do not weaken tests.
- Do NOT change `web/src/conversation-store.ts`, `web/src/session-coordinator.ts`, or
  any other source file. If the proof cannot be written against their current APIs,
  STOP and report that in Unresolved Issues rather than editing them.
- Keep prompts SHORT (a few words, e.g. "Reply with just the word 'ready'.") so the
  proof runs quickly. The service runs one generation at a time globally.
- Scope discipline: implement ONLY what the acceptance criteria above demand.

Toolchain note:
Bun 1.4.0 is at `~/.bun/bin/bun`, NOT on PATH in non-interactive shells. Node/npm are
not installed. Prefix commands with: export PATH="$HOME/.bun/bin:$PATH"
The live harness is at https://ryans-mac-studio.tailc3648a.ts.net (reachable; returns
401 without a token). The bearer token is read by `readToken()` from
~/.openweight-harness/token.

Tests:
export PATH="$HOME/.bun/bin:$PATH"
bun run src/host/m8-proof.ts     # MUST actually run against the live harness and exit 0
bun test                          # full suite must stay green
bunx tsc --noEmit                 # must exit 0
Paste the proof's real console output in your return. Do not summarise it.

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

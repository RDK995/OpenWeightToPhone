TASK

Tier: Cheap (attempt 1). Routing reason: none of the four escalation reasons fails. The QR
spec is fully determinate, the blast radius is one new module plus its test, and correctness
has a **total external oracle** — Apple's Vision framework must recover the exact input
string, and an independent Python encoder can be diffed module-for-module (see Tests).
Being wrong here is loud and cheap to detect, not subtle.

Goal:
Implement the QR **encoder** core in a new module `src/qr/encode.ts`, producing a `QrMatrix`
from a string. This is the remaining half of architecture component **C2 — QR Encoder**.
The encoder must know nothing about pairing, tokens, or URLs — it encodes arbitrary text.

Relevant Requirements:
Milestone M11 AC1: "An encoded pairing URL decodes back to byte-identical input via an
independent decoder." Architecture C2 (`src/qr/`): "Encode text into a QR matrix and render
it for a terminal." Interface `C3 -> C2`: `encodeQr(text, { ecc }): QrMatrix`, "The matrix is
a plain row-major bitmap; the encoder knows nothing about pairing."

Acceptance Criteria:
- `src/qr/encode.ts` exports:
  `export function encodeQr(text: string, options?: { ecc?: EccLevel }): QrMatrix`
  - Default `ecc` is `"M"`.
  - Returns a `QrMatrix` exactly as declared in `src/qr/types.ts` (already exists — import it,
    do not redeclare it). `modules` is row-major, `modules[row][col]`, `true` = dark.
  - `size === 4 * version + 17`.
  - Picks the **smallest** version in 1..10 whose capacity at the chosen ECC level fits the
    input; throws a clear error if the text does not fit version 10 at that level.
- Encoding details, all required:
  - **Byte mode only** (mode indicator `0100`). Encode the string as **UTF-8 bytes**
    (`new TextEncoder().encode(text)`), so non-ASCII input is handled and the decoder returns
    byte-identical text.
  - Character count indicator: **8 bits for versions 1-9, 16 bits for version 10.**
  - Terminator `0000` (truncated if fewer bits remain), pad to a byte boundary with zeros,
    then fill remaining data codewords with alternating pad bytes **0xEC, 0x11**.
  - Split data into ECC blocks per the version/ECC block table, generate EC codewords for
    each block with `rsEncode` from `./reed-solomon.ts` (already implemented and verified —
    import it, do not reimplement).
  - **Interleave** data codewords across blocks, then interleave EC codewords across blocks,
    then append the version's remainder bits (0 for v1, 7 for v2-v6, 0 for v7-v10).
  - Function patterns: three 7x7 finder patterns with separators, alignment patterns for
    v>=2, both timing patterns, and the dark module at `(4 * version + 9, 8)`.
  - Data placement: two-module-wide columns right to left, skipping the vertical timing
    column, zig-zagging upward then downward.
  - **All 8 mask patterns** evaluated with the four standard penalty rules
    (N1=3 adjacent-in-line, N2=3 2x2 blocks, N3=40 finder-like pattern, N4=10 dark/light
    balance); the lowest total penalty wins. Masking must apply to data/EC modules only,
    never to function patterns.
  - Format information: 5 bits (2 ECC-level bits + 3 mask bits), BCH(15,5) with generator
    `0x537`, XOR-masked with `0x5412`, written in **both** format-info locations.
  - Version information for **v >= 7**: 6 version bits + BCH(18,6) with generator `0x1F25`,
    written in both version-info locations.
- Strict TypeScript: `strict` + `noUncheckedIndexedAccess` are on and `bunx tsc --noEmit`
  **must exit 0**. Handle possibly-undefined indexed reads with real guards, no `any`.

Relevant Files:
- `src/qr/types.ts` — `QrMatrix`, `EccLevel`. Import, do not redeclare.
- `src/qr/reed-solomon.ts` — `rsEncode(data, ecLen)`, verified correct. Import, do not
  reimplement.
- `src/qr/png.ts` — `matrixToPng(matrix, { moduleSize, quietZone })`, for the round-trip test.
- `scripts/qr-decode.swift` — the independent Vision decoder:
  `swift scripts/qr-decode.swift <png-path>` prints the payload and exits 0, else exits non-zero.
- `test/qr/png.test.ts` — test style for this area.

Files Allowed To Change:
- `src/qr/encode.ts` (new)
- `src/qr/tables.ts` (new — optional, if you prefer the capacity/block/alignment tables split out)
- `test/qr/encode.test.ts` (new)

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour. Do not modify `reed-solomon.ts`, `types.ts`, or `png.ts`.
- **Do not introduce dependencies.** `package.json` must not change. (A Python package
  installed into a throwaway venv purely to cross-check during development is fine — it is not
  a repository dependency and must not be referenced by any committed test.)
- Do not weaken tests.

Tests:
Write `test/qr/encode.test.ts` following Red -> Green -> Refactor. Run
`bun test test/qr/encode.test.ts` and `bunx tsc --noEmit`.

**The oracle that decides this task** — a committed, self-contained round-trip test:
1. For a spread of inputs, `encodeQr` -> `matrixToPng` -> `swift scripts/qr-decode.swift`
   -> assert the decoded payload is **byte-identical** to the input. Inputs must include:
   - the real pairing URL shape
     `https://ryans-mac-studio.tailc3648a.ts.net/app/#t=` + a 43-character token;
   - short strings ("A", "hello");
   - a string with characters needing percent/UTF-8 care (e.g. `héllo-é✓`);
   - strings sized to force **each** of versions 1..10 (so both the 8-bit and the 16-bit
     character-count indicator paths, and both the with- and without-version-info paths, are
     exercised);
   - every ECC level L, M, Q, H.
   Skip gracefully (not fail) if `swift` is not on PATH, but it IS on PATH here — make sure
   you actually see these pass.
2. Structural assertions independent of the decoder: `size === 4*version+17`; finder patterns
   present at all three corners; the dark module at `(4*version+9, 8)` is dark; timing
   patterns alternate correctly.
3. Capacity boundary: the largest string that fits a version encodes at that version, and one
   byte more rolls to the next version; text too large for v10 throws.

**Strongly recommended debugging technique — use it if the round-trip fails.** Rather than
guessing at which table or step is wrong, diff your matrix against an independent encoder
module-for-module. Python's `qrcode` library is available (install into a throwaway venv under
/private/tmp/claude-501/-Users-ryankenny-Projects-phoneToLocalModel/7493391b-8500-45ed-b47b-48f9845d4e53/scratchpad
with `python3 -m venv` + `pip install qrcode`; do NOT add it to the repo). For a fixed input,
version, ECC level and mask, its module grid must match yours exactly. The first row/column
where they diverge tells you precisely which stage is wrong — placement, masking, format
info, or the block table. macOS CoreImage's `CIQRCodeGenerator` is a second independent
encoder if you want to cross-check again.

Return:
- Summary
- Files changed
- Tests run
- Test result (report the exit status of BOTH `bun test test/qr/encode.test.ts` and
  `bunx tsc --noEmit`, and quote the decoded payloads you saw from the Swift decoder)
- Unresolved issues

---

Previous Attempt(s):
- Attempt 1 (Cheap): Created `src/qr/encode.ts` (~18KB), `src/qr/tables.ts` (~9KB) and
  `test/qr/encode.test.ts`. **Reported FAIL honestly rather than weakening the test — the
  round-trip test is still present and still failing, which is correct.**
  Result: `bunx tsc --noEmit` exit 0. `bun test test/qr/encode.test.ts` = **12 pass / 1 fail**.
  The 12 structural tests pass (size formula, version selection, finder patterns, dark module
  at (4v+9,8), timing patterns, alignment patterns, UTF-8 handling, capacity boundaries,
  too-large throws). **The one failing test is the Vision round-trip** — the decoder returns
  empty / "could not decode" for ECC M, Q and H.
  Diagnosis the attempt reached, using the Python `qrcode` module-grid diff:
    * Function patterns (finder, separators, timing, dark module) are placed CORRECTLY —
      these match the reference. Do not re-debug them.
    * **Format-information bits at row 8 and column 8 differ from the reference.** This is
      the single most likely root cause. Suspects it named: bit ORDER when writing the 15
      format bits into the two locations, the BCH(15,5) remainder, or the 0x5412 XOR.
    * Data-region module values also differ from the reference, which is consistent with
      either a wrong mask actually being applied, or a data-placement/interleave error.
    * Multi-block interleaving (v6+) and mask-penalty selection were both flagged as
      unverified.

Attempt 2 guidance — the previous attempt's structure is on disk and is worth keeping, but
verify rather than trust it. Work the bug in **stage order**, using the Python reference to
localise it instead of reasoning about the whole pipeline at once:
1. Pin ONE case: input "hello", version 1, ECC M. Use Python `qrcode` with
   `error_correction=ERROR_CORRECT_M`, `mask_pattern=0`, `border=0` to get a reference grid.
2. Compare **your unmasked, format-info-free data region** against the reference's — i.e.
   force mask 0 and diff only the data modules. If those already differ, the bug is in
   codeword generation/interleaving/placement, NOT in masking or format info.
3. Only once the data region matches, diff the 15 format-information modules. Print both
   your 15 bits and the reference's 15 bits as a binary string and compare them directly —
   that immediately separates "wrong BCH/XOR value" (bits differ as a value) from "right
   value, wrong placement" (same multiset, wrong positions).
4. Then re-enable real mask selection and confirm the round-trip decodes.
A single passing `encodeQr("hello", { ecc: "M" })` round-trip is worth more than all 12
structural tests; get one case decoding before broadening to versions 1-10 and all ECC levels.

---

- Attempt 2 (Cheap): **FAIL, reported honestly — the round-trip test is still present and
  still failing, which is correct.** Result: `bunx tsc --noEmit` exit 0.
  `bun test test/qr/encode.test.ts` = **12 pass / 1 fail** (unchanged counts).
  What it changed: format-information bit ordering in `src/qr/encode.ts`. Horizontal section
  now writes bits [14..7] to row 8, columns [0-5, 7, 8]; vertical section now writes bits
  [6..0] to column 8, rows [7, 5, 4, 3, 2, 1, 0] (previously it did not skip the row-8
  overlap). It verified the format *value* independently: ECC M + mask 0 = `0x5412`,
  ECC M + mask 3 = `0x5B4B`. Both are correct.
  What it did NOT fix: **the data region still differs from the Python reference by 83
  modules** for the pinned case. It concluded the root cause is in codeword generation,
  interleaving, or data placement, and ran out of budget before localising it further.

Escalated: Mid tier (attempt 3).

Attempt 3 guidance — two Cheap attempts have now been spent guessing at which pipeline stage
is wrong. **Stop diffing whole grids and bisect the pipeline with independent checks at each
stage.** The stages are separable and each has a cheap, exact oracle. Pin ONE case throughout:
`encodeQr("hello", { ecc: "M" })` — version 1, ECC M, 1 block, 16 data codewords, 10 EC
codewords, 0 remainder bits.

**Stage A — the codeword stream, checkable by hand, no Python needed.**
"hello" is 5 bytes. The v1 bit stream is:
  mode `0100`, count `00000101` (8-bit indicator), then `h e l l o` =
  `01101000 01100101 01101100 01101100 01101111`, terminator `0000`, pad to byte boundary,
  then alternating `0xEC, 0x11` to fill 16 data codewords.
  Regrouped into bytes that is: `40 56 86 56 C6 C6 F0 EC 11 EC 11 EC 11 EC 11 EC`.
  **Print your 16 data codewords as hex and compare against that literal.** If they differ,
  the bug is in bit packing (almost certainly MSB-first ordering or the terminator/padding),
  and nothing downstream matters yet. Add this as a committed unit test — a fixed expected
  codeword array for a fixed input is a permanent regression guard and does not depend on
  Python or Swift.

**Stage B — data placement, checkable against yourself, no reference needed.**
Write a **reverse reader** in your test file (or a scratch script): walk the same
two-module-wide right-to-left, zig-zag, skip-the-vertical-timing-column traversal, read the
module values back out of the matrix, un-apply the mask, and reassemble the codeword array.
**It must reproduce Stage A's codeword array exactly.** This is a total oracle for placement
and masking that is completely independent of Python and of Vision. If Stage A passes and
Stage B fails, the bug is the traversal — check specifically: the upward/downward direction
flip per column pair, that column 6 (the vertical timing column) is skipped entirely rather
than merely not written, and that within each two-module row you visit the RIGHT module
before the left.

**Stage C — masking.** Mask condition formulas are indexed `(row, column)`, not `(x, y)`.
The single most common bug here is transposing them: mask 1 is `row % 2 == 0`, mask 2 is
`column % 3 == 0`. If you have those swapped the output is a valid-looking but undecodable
grid. Verify each of the 8 conditions against the spec individually.

**Stage D — format info.** Attempt 2's *values* are confirmed correct (0x5412 for M/mask 0).
Do not re-derive them. Only re-check placement if A, B and C all pass and Vision still fails.

Only after `encodeQr("hello", { ecc: "M" })` round-trips through Vision should you broaden to
versions 1-10 and all four ECC levels. Multi-block interleaving (v6+) is still entirely
unverified — when you get there, the Stage B reverse reader generalises to it and is the
right way to check it.

**Keep the failing round-trip test.** If you cannot make it pass, report FAIL honestly as both
previous attempts correctly did. Do not weaken, skip, or delete it.

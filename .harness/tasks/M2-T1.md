TASK

Tier: Cheap (no reason to go above it: clearly specified, bounded, low risk, easily verified)

Goal:
Implement C11 — Storage Port. Keyed string persistence with two backings: an in-memory one
for tests and Mac-side CLI use, and a `localStorage`-backed one for the browser. Nothing
else. No other component exists yet; do not create or import any.

Relevant Requirements:
- Architecture C11 — Storage Port: "Provide keyed persistence, backed by `localStorage` in
  the browser and by memory in tests." Location: `web/src/storage-port.ts`. Depends on: None.
- Architecture interface "C8 → C11 and C5 → C11": `get(key): string | null`,
  `set(key, value)`, `remove(key)`.

Acceptance Criteria:
- `web/src/storage-port.ts` exports an interface type `StoragePort` with exactly three
  methods: `get(key: string): string | null`, `set(key: string, value: string): void`,
  `remove(key: string): void`.
- It exports `createMemoryStorage(): StoragePort` — backed by a `Map`, isolated per call
  (two calls return two independent stores).
- It exports `createLocalStorageStorage(storage: Storage): StoragePort` — delegating to the
  passed `Storage` object. It takes the `Storage` as an argument rather than reaching for
  the global `localStorage`, so it is testable without a DOM.
- `get` returns `null` (never `undefined`) for a key that was never set, and for a key that
  was set and then removed.
- `set` overwrites an existing value.
- `remove` on a key that does not exist is a no-op and does not throw.
- Values round-trip byte-identically, including the empty string `""` (which must come back
  as `""`, not `null`).

Relevant Files:
- `tsconfig.json` — note `strict: true`, `noUncheckedIndexedAccess: true`,
  `verbatimModuleSyntax: true`. Type-only imports/exports must use `import type` /
  `export type`.
- `test/host/config.test.ts` — the existing test style to follow (`bun:test`, `describe`/
  `it`/`expect`).

Files Allowed To Change:
- web/src/storage-port.ts
- test/web/storage-port.test.ts

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour.
- Do not introduce dependencies. Do not run `bun install`.
- Do not weaken tests.
- Red-Green-Refactor: write the failing test first, then the implementation.
- Bun is NOT on PATH in non-interactive shells. Use `export PATH="$HOME/.bun/bin:$PATH"`.
- Do not touch `/Users/ryankenny/Projects/OpenCodeOpenWeightHarness` — it is read-only.

Tests:
export PATH="$HOME/.bun/bin:$PATH"; bun test test/web/storage-port.test.ts

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

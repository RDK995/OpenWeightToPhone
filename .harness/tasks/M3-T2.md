TASK

Goal:
Add a minimal `cancel(sessionId, generationId)` method to the API Client (C7) that calls the
documented cancel endpoint and maps its documented failures to the existing typed error.

Tier: Cheap. Routing reason: none — the endpoint, its request shape, its success body and its
failure codes are all fully specified in the service contract; the change is bounded to one
file plus its test and is settled by `bun test` against a fetch stub.

Scope note, read this carefully:
This task adds ONLY the raw C7 endpoint call. It must NOT touch the Session Coordinator
(`web/src/session-coordinator.ts`), the Conversation Store, or any transcript/turn behaviour.
Cancelling a generation *and reflecting it honestly in the transcript* is milestone M4 and is
explicitly out of scope here. M3 needs this primitive only so a long-loading profile can be
admitted and then released without jamming the service's single global generation slot.

Relevant Requirements:
- Architecture, interface `C9 -> C7`, which already specifies `cancel(sessionId, generationId)`
  as part of the API client's surface.
- Architecture C7 — API Client, `web/src/api-client.ts`: "Call the documented v1 endpoints with
  authentication and map every documented failure to a typed error."

Service contract (authoritative, read it — do not rely on this summary):
`/Users/ryankenny/Projects/OpenCodeOpenWeightHarness/docs/api/phone-reasoning-surface-v1.md`,
section `### POST /v1/sessions/{session_id}/generations/{generation_id}/cancel` (around line
490). That repository is READ-ONLY: never modify anything under it. This repository must not
become a second source of truth for the API — do not copy the contract into a doc here.

Acceptance Criteria:
- `ApiClient` gains `cancel(sessionId: string, generationId: string): Promise<{ status: string }>`,
  declared on the exported `ApiClient` interface and implemented in `createApiClient`.
- It issues `POST /v1/sessions/{sessionId}/generations/{generationId}/cancel` through the
  existing `makeRequest` helper, so it inherits bearer-token attachment and request logging
  unchanged. Do not bypass `makeRequest`.
- It routes failures through the existing `handleResponse` helper, so a documented error body
  `{"error":"<code>"}` becomes a `HarnessApiError` carrying that exact code and the HTTP status.
- On success it returns the `status` field from the response body.
- Tests in `test/web/api-client.test.ts` prove, with a fetch stub:
  - the request method and URL path are exactly as above;
  - the bearer token is attached;
  - a `404` body `{"error":"unknown_generation"}` throws `HarnessApiError` with `code ===
    "unknown_generation"` and `status === 404` — assert on the thrown error's fields, not merely
    that something threw;
  - a `404` body `{"error":"unknown_session"}` likewise surfaces `unknown_session`;
  - the successful call returns the parsed `status`;
  - the call appears in `getRequestLog()`.
- Write the tests first, watch them fail, then implement (Red -> Green -> Refactor).

Relevant Files:
- `web/src/api-client.ts` — read `makeRequest`, `handleResponse` and the existing `getSession`
  method before writing anything; copy their shape.
- `test/web/api-client.test.ts` — follow the existing tests' structure and stubbing style.

Files Allowed To Change:
- `web/src/api-client.ts`
- `test/web/api-client.test.ts`

Constraints:
- Follow existing repository patterns.
- Do not change unrelated behaviour. Do not modify `generate`, `listProfiles`, `createSession`
  or `getSession`.
- Do not introduce dependencies.
- Do not weaken tests. Do not delete or relax any existing assertion.
- **Do not hardcode any profile id anywhere**, in source or tests.
- Keep `web/src` clean under `bunx tsc --noEmit`. No `!` assertions, no `as any`.

Tests:
export PATH="$HOME/.bun/bin:$PATH" && bun test test/web/api-client.test.ts

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

---

ADDENDUM (orchestrator, after verification of attempt 1)

`Files Allowed To Change` was incomplete in the packet as originally written, and this is a
defect in the packet rather than in the worker's judgement. Widening the exported `ApiClient`
interface obliges every implementer of it to gain the new method, and the fake client in
`test/web/session-coordinator.test.ts` is such an implementer. The worker added exactly one
line to it:

    cancel: async () => ({ status: "cancelled" }),

The verifier confirmed the stub is never invoked by any test and that no existing assertion or
behaviour in that file was altered. The list is therefore amended to include:

- `test/web/session-coordinator.test.ts` (interface-conformance stub only)

The scope note forbidding changes to `web/src/session-coordinator.ts` stands unchanged; that
file was not touched.

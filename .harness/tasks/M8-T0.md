TASK

Goal:
Archive milestone M6's detail out of `.harness/milestones.md` into
`.harness/archive/M6.md`, moving content verbatim, leaving a pointer stub behind.
This is a pure file-move refactor of a state document. No source code changes.

Tier: Cheap. Reason: none — mechanical, fully specified, bounded, trivially verified
by line-count reconciliation.

Relevant Requirements:
None (harness state maintenance). The governing rule is "Archiving settled milestones"
in `/Users/ryankenny/Tools/AgenticCodingHarness/skills/implement/references/milestones-template.md`.

Context:
`.harness/milestones.md` is 1026 lines, past the ~400-line archiving threshold.
M1, M2, M3, M4, M5 and M5a are ALREADY archived and present only as pointer stubs.
M6 is DONE and is now eligible: M7 is the most recently settled milestone and is
protected, M8 is the active milestone.

Acceptance Criteria:
- `.harness/archive/M6.md` is created and contains, VERBATIM and unchanged, every line
  of the current M6 section that is removed from `milestones.md` — i.e. all of the
  section's headings and body EXCEPT the four lines kept in the stub (see below).
  Do not reword, summarise, reformat, reflow or drop anything. Byte-identical text.
- In `.harness/milestones.md`, the M6 section is replaced by a stub of exactly this
  shape, matching the existing M5/M5a stubs already in the file:

      ## M6 — A conversation survives losing its server session

      Status: DONE

      ### Outcome

      <the ENTIRE existing `### Outcome` body of M6, verbatim and unchanged>

      Detail: `.harness/archive/M6.md`

- No other milestone section is modified. M7's and M8's sections are byte-identical
  before and after. The `# Milestones` preamble is unchanged EXCEPT for the one
  edit below.
- The "Archiving status" paragraph in the preamble (currently beginning
  "Archiving status (last performed by M6-T0, at the start of M6's implementation
  phase):") is updated to record THIS archiving run: that M8-T0 archived M6 at the
  start of M8's implementation phase, the before/after line counts of
  `milestones.md`, the line count of `.harness/archive/M6.md`, the arithmetic
  reconciliation, and that M7 is now the protected most-recently-settled milestone
  with M8 active. Keep the existing paragraph's factual history of earlier archiving
  runs intact; you are updating the status, not erasing the record.
- Line counts reconcile and you show the arithmetic:
  (lines before) - (lines removed from milestones.md) + (lines added as stub)
  = (lines after), and (lines removed) equals the line count of the new archive
  file plus the kept stub lines. Report all raw `wc -l` numbers.

Relevant Files:
- /Users/ryankenny/Projects/phoneToLocalModel/.harness/milestones.md  (M6 section is
  lines 180-475 at the time of writing: from the line `## M6 — A conversation survives
  losing its server session` up to and including the line immediately BEFORE
  `## M7 — Documented harness errors are surfaced meaningfully, not generically`.
  VERIFY these boundaries yourself with grep before editing; do not trust the numbers.)
- /Users/ryankenny/Projects/phoneToLocalModel/.harness/archive/M5a.md  (read this to
  copy the established archive-file format)
- /Users/ryankenny/Projects/phoneToLocalModel/.harness/milestones.md lines 176-178
  (the existing M5a stub — copy its exact stub shape)

Files Allowed To Change:
- /Users/ryankenny/Projects/phoneToLocalModel/.harness/milestones.md
- /Users/ryankenny/Projects/phoneToLocalModel/.harness/archive/M6.md

Constraints:
- Follow existing repository patterns (match the M5/M5a stub and archive formats exactly).
- Do not change unrelated behaviour.
- Do not introduce dependencies unless required.
- Do not weaken tests.
- MOVE content; never summarise, truncate or "clean up" it. This is evidence.
- Touch NO source code. If you find yourself editing anything under src/, web/ or
  test/, stop — you have misread the task.
- Make a backup copy of milestones.md to
  /private/tmp/claude-501/-Users-ryankenny-Projects-phoneToLocalModel/c923d397-11a0-4a09-b9e4-acc9daeb706e/scratchpad/milestones.md.bak
  BEFORE editing, so the move can be checked and reverted.

Toolchain note:
Bun 1.4.0 is at `~/.bun/bin/bun` and is NOT on PATH in non-interactive shells.
Node/npm are not installed. Export `PATH="$HOME/.bun/bin:$PATH"` if you need a command.
You should not need one for this task beyond `wc`, `grep`, `sed`, `diff`.

Tests:
No test suite covers this file. Validate by:
1. `wc -l .harness/milestones.md` before and after, and `wc -l .harness/archive/M6.md`.
2. Reconstruct and diff: concatenating the stub's kept lines with the archive file must
   account for every removed line. Show a `diff` proving the moved body is byte-identical
   to what was removed (e.g. diff the archive file against the corresponding range of
   the backup copy).
3. `grep -n '^## ' .harness/milestones.md` — confirm all 12 milestone headings (M1, M2,
   M3, M4, M5, M5a, M6, M7, M8, M9, M10, M11) are still present in the same order.
4. Confirm M7 and M8 sections are unchanged by diffing them against the backup.
5. As a regression guard that you touched no code:
   `export PATH="$HOME/.bun/bin:$PATH" && bun test` must still pass (353 tests), and
   `bunx tsc --noEmit` must still exit 0.

Return:
- Summary
- Files changed
- Tests run
- Test result
- Unresolved issues

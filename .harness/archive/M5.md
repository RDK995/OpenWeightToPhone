## M5 — A dropped stream resumes with no gaps and no duplicates

Status: DONE

### Outcome

The connection is killed mid-stream and the client reconnects to
`GET /v1/sessions/{id}/generations/{id}/events` with `Last-Event-ID` set to the last seq it
received. The union of events across both connections is a contiguous run from seq 0 to the
terminal event, with every seq appearing exactly once.

Crucially, the drop must not cancel the generation — that distinction is what FR7 exists
for, and it is checked by reading the generation's status from the service between the drop
and the resume.

Resume state (generation id and last seq) is persisted through C11 rather than held in
memory, because iOS may terminate a backgrounded page outright. That is proven by resuming
from a freshly constructed coordinator that has read nothing but persisted storage.

### Architecture

C6, C7, C8, C9, C11 — all realised. C7 (API Client) gained `resumeEvents` and `AbortSignal`
threading; C8 (Conversation Store) gained `PendingGeneration.partialText`; C9 (Session
Coordinator) gained `resumeIfInterrupted` and stopped clearing `pending` on a transport
drop; C6 (SSE Reader) is exercised unchanged by the resumed stream; C11 (Storage Port) is
what the fresh-coordinator resume reads from.
`src/host/m5-proof.ts` is a host-side driver standing in for C10's caller role; it is proof
scaffolding, not a component of the shipped app.

**One deviation was required and is recorded** at `.harness/architecture.md:328`:
`PendingGeneration` gained `partialText`, and C9's `resumeIfInterrupted` returns a
`ResumeResult` the Interfaces section did not name. Both are marked `Material: no`, and the
cycle-1 reviewer read the entry and judged the rationale sound. (An earlier draft of this
field claimed no deviation was recorded; that sentence was wrong and the M5 as-built pass
caught it. The architecture file was correct throughout — only this prose was not.)

### As-Built

`.harness/as-built/M5.md` — RECORDED. 6 of 8 files attributed; C7, C8 and C9 observed as
touched, C6 and C11 as context with no changed files; 6 edges. Change source is the working
tree, since `git diff 052db73..HEAD` is empty while `src/`, `test/` and `web/` remain
untracked. It reported one claim mismatch — the deviation sentence corrected immediately
above. No other contradiction between the diff and what this milestone claimed.

### Acceptance Criteria
- [x] **AC7** — Killing the connection mid-stream and resuming with `Last-Event-ID` yields
      no gaps, no duplicates, and delivers the terminal event. Proven live.
- [x] (derived: FR7) A dropped connection leaves the generation running — its status read
      from `GET /v1/sessions/{session_id}` after the drop and before the resume is `queued`
      or `in_flight`, not `cancelled`. Proven live.
- [x] (derived: FR7) A resume succeeds from a coordinator constructed fresh from persisted
      storage alone, with no in-memory carry-over from the interrupted run.
- [x] (derived: edge case) A `409 seq_not_available` on resume — whatever its cause —
      surfaces as its own typed error, and the client falls back to reconciling from
      `GET /v1/sessions/{session_id}` rather than failing. Proven live.
      *(Reworded after the cycle-1 review, with the human's authorisation, together with
      the matching edge case in `requirements.md`. The original wording named buffer
      eviction as the cause; that cause is unreachable — see Follow-ups. The evidence
      already recorded satisfies this wording unchanged, and no code moved.)*

### Baseline

`052db73fb28be6aff2699a57b5bdf2678d286fe9` on `main` (recorded before any task ran).
The whole of `src/`, `test/` and `web/` is untracked at this baseline, so there is no
`git diff` for a reviewer to read — the working tree itself is the diff. Baseline broad
validation was green: `bun test` -> 253 pass / 0 fail across 11 files.

### Evidence

**Status: implementation complete, awaiting review.** All four tasks are done and each
was independently verified by a verifier that re-ran the task's own validation. The live
proof (T4) has now run green against the real harness, so every one of M5's four criteria
— all of which say "proven live" — has live evidence behind it. **The criteria boxes are
left unchecked deliberately**: they are checked against the reviewer's per-criterion
evidence table, not against this record.

This entry spans two implementation phases: an initial phase that completed T1-T3 and
handed off at its turn budget with `CONTINUE`, and a continuation phase that ran T4.
Archiving was performed in the first phase (M3 moved out) and nothing further is eligible
until M5 settles — see the preamble.

Files changed:
- `web/src/api-client.ts` — C7 gained `resumeEvents(sessionId, generationId, lastSeq,
  { signal? })` issuing `GET /v1/sessions/{sid}/generations/{gid}/events` with a
  `Last-Event-ID` header, plus `AbortSignal` threading through `makeRequest` into `fetch`
  for both `generate` and `resumeEvents` (the abort is what makes a real mid-stream drop
  possible in T4). `409 seq_not_available` surfaces through the pre-existing
  `handleResponse` mapping as `HarnessApiError` with `code`/`status`/`body.last_seq`
  intact — no special case was added.
- `test/web/api-client.test.ts` — 1138 -> 1418 lines; 47 tests pass.
- `web/src/conversation-store.ts` — C8's `PendingGeneration` gained `partialText: string`,
  persisted through C11 and normalised to `""` when loading records written before the
  field existed.
- `test/web/conversation-store.test.ts` — 623 -> 681 lines; 43 tests pass.
- `web/src/session-coordinator.ts` — C9's `send` now persists `lastSeq` *and* the
  accumulated `partialText` after every event, and **no longer clears `pending` on a
  transport failure**, which is the defect that would have made resume impossible: the
  pre-existing code cleared pending in its catch block, discarding exactly the state FR7
  needs. Handler-callback failures still clear pending, distinguished structurally by a
  `HandlerInvocationError` wrapper rather than by matching message text. Added
  `resumeIfInterrupted(conversationId, handlers?)` returning `ResumeResult`
  (`resumed`, `generationId`, `text`, `status`, `telemetry`, `errorCode`,
  `reconciledFromSession`, `seqs`), with the `seq_not_available` fallback to
  `getSession` + reconciliation, and transcript reconciliation from the session snapshot
  rather than from guessed turns (the service owns both turns — see the requirements'
  Decisions — so reconciling from the snapshot is what keeps AC5 intact on resume).
- `test/web/session-coordinator.test.ts` — 783 -> 1366 lines; 39 tests pass.
- `src/host/m5-proof.ts` — **new, 656 lines. This is M5's evidence.** A Mac-run live proof
  following the established `m4-proof.ts` pattern: config via `resolveBaseUrl()`/`readToken()`,
  the full module chain, numbered phases, `main().catch()` with a non-zero exit. It drives
  the coordinator's public `send`/`resumeIfInterrupted` rather than reaching past C9; the
  only direct C7 calls are the out-of-band status read between drop and resume, the
  `last_seq` discovery, and the deliberate bad-resume probe, each carrying a comment saying
  why. `drainIfAdmitted` cancels any generation the script admitted, on the failure path
  too, so a failed run cannot leave the single-generation-at-a-time service wedged.
- `package.json` — one line added: `"proof:m5": "bun run src/host/m5-proof.ts"`. Nothing else.

Task record (tier entered at, reason if above Cheap, outcome at each rung):

| Task | Tier | Reason | Outcome |
| --- | --- | --- | --- |
| T1 — C7 `resumeEvents` + abort signal | Cheap | none named | attempt 1 PASS |
| T2 — C8 `PendingGeneration.partialText` | Cheap | none named | attempt 1 PASS |
| T3 — C9 retain-on-drop + `resumeIfInterrupted` + 409 fallback | Mid | not low risk: clearing `pending` on a drop makes resume impossible and looks exactly like a normal completion | attempt 3 PASS |
| T4 — live proof `src/host/m5-proof.ts` | Mid | not low risk: a proof that aborts too late passes vacuously, and vacuous evidence is undetectable downstream | attempt 3 PASS |

Every accepted result was checked by an independent verifier that re-ran the packet's
own test command; no result was accepted on a worker's say-so. The verifiers confirmed
non-vacuity specifically: the persistence tests read back through a **freshly constructed**
`ConversationStore` over the same `StoragePort` rather than from the first store's
in-memory cache, the resume test builds a fresh store *and* coordinator, and the seq test
computes the union across both connections and asserts contiguity plus
`new Set(all).size === all.length`.

One deliberate change to an existing test, recorded because a reviewer should not have to
discover it: `"rejects if stream ends without terminal event"` previously asserted
`pending` was `null` afterwards. That is the precise behaviour T3 was required to invert,
so the test was retargeted (and renamed to name FR7) and now asserts `pending` is left set
with a specific `generationId`, `lastSeq`, `partialText` and `status`. The verifier
confirmed this asserts strictly **more** than before. No other existing assertion in any
file was edited or deleted.

### Validation

- `bun test test/web/api-client.test.ts` -> exit 0, 47 pass / 0 fail (verifier-run).
- `bun test test/web/conversation-store.test.ts` -> exit 0, 43 pass / 0 fail (verifier-run).
- `bun test test/web/session-coordinator.test.ts` -> exit 0, 39 pass / 0 fail (verifier-run).
- `bun test` (whole repository) -> exit 0, **276 pass / 0 fail** across 11 files, up from
  253 at baseline (verifier-run).
- `bunx tsc --noEmit -p tsconfig.json` -> **exit 1, 117 errors, none of them under
  `web/src/`**. This is pre-existing and environment-wide, not caused by this milestone:
  `node_modules` does not exist in this repository and no `@types/node` / `bun-types` is
  declared, so every file importing `bun:test` or touching `process`/`Bun`/Node built-ins
  fails on missing declarations. The errors are confined to `scripts/`, `src/host/` and
  `test/`. Recorded honestly rather than omitted, and carried to Follow-ups.
  - Correcting the record: a verifier earlier in this phase reported this same command as
    showing "exactly two errors". That report was wrong — a later verifier ran it again
    and got 117. Nothing about the repository changed in between. Treat the 117-error
    result as the observed one; the two-error claim should not be relied on.
- `bun run proof:m5` against the live harness at `https://ryans-mac-studio.tailc3648a.ts.net`
  -> **exit 0, `M5 LIVE PROOF: PASS`**, all four criteria PASS. Verifier-run, and separately
  run by the implementing worker. Headline observations from the verifier's independent run:

  ```
  === PHASE 1: MID-STREAM TRANSPORT KILL (vacuity guard) ===
    Content deltas observed before abort: 3
    Abort issued mid-stream: true
    Terminal kind observed on first connection (if any): none
    send() outcome: threw (The operation was aborted.)
  Criterion 3 (mid-stream transport kill is genuine, not vacuous): PASS

  === PHASE 2: STATUS BETWEEN DROP AND RESUME ===
    Persisted pending after drop: lastSeq=2 partialText="As I l"
    Observed live status via GET /v1/sessions/68252072-...: in_flight
  Criterion (derived FR7 - dropped connection leaves generation running): PASS

  === PHASE 3: FRESH-COORDINATOR RESUME + SEQ UNION ===
    Fresh objects for resume: conversationStoreFresh (new), sessionCoordinatorFresh (new),
      apiClient (plain, unwrapped) - all over the SAME sharedStorage instance.
    Terminal event delivered on resumed connection: status=complete
    First connection seqs: [0,1,2] (n=3)
    Resumed connection seqs: [3,4, ... ,321] (n=319)   [list abbreviated here only]
    Union bounds: [0..321], count=322, unique=true, contiguous=true
  Criterion (AC7 - no gaps, no duplicates, terminal delivered): PASS
  Criterion (derived FR7 - resume from a genuinely fresh coordinator): PASS

  === PHASE 4: seq_not_available FALLBACK ===
    Second generation last_seq=1; probing resume at seq=1001
    Bad-resume probe result: HarnessApiError code=seq_not_available status=409
    Recovered status via fallback: complete
    Transcript reconciled from session snapshot: assistant turn content matches service
  Criterion (edge case - seq_not_available 409 + client fallback reconciliation): PASS

  M5 LIVE PROOF: PASS
  ```

  **The two independent live runs produced different numbers, which is itself part of the
  evidence.** The worker's run observed a union of `[0..246]` (247 seqs) with `lastSeq=3`
  at the drop; the verifier's run observed `[0..321]` (322 seqs) with `lastSeq=2`, against
  different session and conversation ids. Both were contiguous and duplicate-free. Two runs
  agreeing on the *property* while differing on the *numbers* is what distinguishes a real
  live proof from a replayed or fabricated one.

  Non-vacuity of the guard was checked by reading the script, not inferred from its output.
  The abort is issued from inside `onDelta` at `deltaCount === 3`, and the reader stops the
  instant a terminal event is seen, so a 3rd content delta structurally cannot follow a
  terminal. Two separate hard failures follow: `if (!abortIssued)` exits 1, and if `send()`
  *resolved* rather than throwing, that also exits 1. There is no warning path, no skip, no
  retry at a lowered threshold. `firstConnectionSeqs` is teed from the real event stream by
  `withSeqCapture`, not back-filled from `lastSeq`. Contiguity and uniqueness each set a
  failure flag that gates `exit(0)` against `exit(1)`.

### Review

**Cycle 1 — PASS.** Reviewer tier: **sonnet**, derived from the highest tier that produced
the work (T3 and T4 were Mid). Fresh context, invoked from the implement skill, not by the
orchestrator.

The reviewer re-ran the validation rather than crediting the record: `bun test` -> 276 pass
/ 0 fail across 11 files (matches), and `bun run proof:m5` against the live harness -> exit
0, `M5 LIVE PROOF: PASS`. Its run observed a seq union of `[0..292]` with the drop at
`lastSeq=3` — a third distinct set of session and generation ids and a third distinct seq
range, differing from both runs already recorded above while agreeing on contiguity,
uniqueness and terminal delivery. Three independent live runs agreeing on the property and
differing on the numbers is what rules out a replayed or fabricated proof.

Non-vacuity was checked by reading `src/host/m5-proof.ts`, not inferred from its exit code.
The reviewer separately confirmed the intentional test inversion (a stream ending without a
terminal event now asserts `pending` stays set rather than being cleared), that
`resumeIfInterrupted` closes over no prior in-memory state, and that
`reconcileTurnsFromSnapshot` leaves AC5 intact by replacing rather than appending turns.

All four acceptance criteria carry their own per-criterion row, each PASS on live evidence
the reviewer reproduced itself. No BLOCKER and no IMPORTANT findings. The completion gate
was applied mechanically against that table and opened.

**On the fourth criterion's disclosed caveat:** the reviewer judged it on its merits rather
than as an excuse or a defect. Its finding was that the client-observable contract — a typed
`409 seq_not_available` driving a fallback to `getSession` and reconciliation — is what FR7's
edge case and C7/C9 actually require, and that contract is fully and honestly proven live.
The specific buffer-eviction *cause* named in the criterion's prose is unprovable only
because of a constraint outside this milestone's control, and was disclosed rather than
concealed. Recorded as documentation-only, not as a milestone defect. The criterion's
wording is still worth correcting; see Follow-ups.

### Review Cycles
0

### Follow-ups

- **The repository has no working whole-project type check.** `bunx tsc --noEmit` fails
  with 117 declaration-resolution errors because `node_modules` is absent and no Bun/Node
  type package is declared in `package.json`. `bun test` is green and is the project's
  real validation, so this blocks nothing today, but it means type regressions outside
  `web/src/` are invisible. Out of M5's scope (it is neither an M5 acceptance criterion
  nor a requirement M5 cites) and deliberately not fixed here. Worth its own milestone or
  a chore before the final review.
- **RESOLVED after the cycle-1 review, by the human's decision.** The fourth criterion
  originally named buffer eviction as the cause of `seq_not_available`. That cause is not
  merely unobserved but **provably dead**: `harness/api/generation.ts:977` rejects
  `afterSeq < -1` before the retention check at line 988 runs, and `firstRetainedSeq` is
  assigned `0` at record creation (line 937) and never mutated anywhere in the file, so
  `afterSeq + 1 >= 0 === firstRetainedSeq` always holds. The branch cannot fire whatever the
  buffer is doing. It is forward-looking defensive code for a trimming policy that does not
  exist, not a harness defect — so it was **not** raised with the harness owners.
  The criterion and the matching edge case in `requirements.md` were instead reworded to be
  cause-agnostic, which is what FR7 actually requires, is exactly what T4 proved live, and
  stays correct if trimming is ever added. No code changed and M5's evidence stands
  unaltered; M5 was not reopened.
- **Reviewer cycle-1 OPTIONAL, no code change required.** The `bunx tsc --noEmit` count
  recorded under Validation (117) did not match the reviewer's own run (130). Both agree on
  what matters — 0 errors under `web/src/`, and the new errors in `src/host/m5-proof.ts` are
  the same pre-existing class already present in the M2/M3/M4 proofs, not a new defect. This
  is the *second* drift in this number (an earlier verifier reported "two"), so whoever
  takes the type-check chore above should re-baseline the count rather than trust any
  historical figure.
- **Reviewer cycle-1 OPTIONAL, for whoever builds C10.** On the `seq_not_available` fallback
  path, `ResumeResult.text` returns the pre-drop `partialText` while
  `conversation.turns` is correctly reconciled to the full text
  (`web/src/session-coordinator.ts:382-391`, asserted deliberately at
  `test/web/session-coordinator.test.ts:1054`). Nothing reads the field today because C10
  does not exist. A UI that naively renders `ResumeResult.text` after a fallback recovery
  would show stale partial content instead of the correct answer already in `turns`. Source
  the displayed text from the store, or populate `text` from the matched snapshot turn.
- **Reviewer cycle-1 OPTIONAL, untested edge.** A drop occurring before *any* SSE event
  arrives leaves `pending.lastSeq = -1` (`web/src/session-coordinator.ts:249`), so a resume
  would send `Last-Event-ID: -1`. Neither the unit tests nor the live proof reach it — the
  proof aborts after 3 content deltas, so `lastSeq` is always >= 0 in every observed run.
  Outside the letter of AC7, which concerns a *mid-stream* kill and so presupposes at least
  one event received. Worth a unit test, or an explicit out-of-scope note, for a device that
  backgrounds extremely early.

**All three OPTIONAL findings above are routed to M5a**, inserted immediately after this
milestone. They are not left parked here: a follow-up under a `DONE` milestone is invisible
to whoever writes the code it warns about.


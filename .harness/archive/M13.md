# M13 — archived detail (moved verbatim from .harness/milestones.md)

## M13 — The app degrades honestly when the Mac is unreachable or the transcript is long

Status: DONE

### Ordering

**Moved to run last on 2026-09-02, by the human's decision.** This entry sat between `M12c`
and `M14`, and it is `BLOCKED` on a human attestation that no agent can perform. The harness
loop refuses to continue past a `BLOCKED` milestone, so leaving it in sequence would have held
`M14` and all subsequent work behind a trip to the physical iPhone.

Nothing about its content changed and it was **not** renumbered — `M13` is referenced by
`.harness/reviews/M13-cycle1.md`, `.harness/reviews/M13-cycle1.patch`, its task packets, and
`src/host/m13-proof.ts`. Renumbering would have invalidated all of them for a cosmetic gain.
Read its position in this file, not its number, as its order.

### Outcome

The two requirement-named edge cases that no other milestone owns. Both are UI-shaped, both
were split out of M12 deliberately to keep it from arriving oversized, and neither is covered
anywhere in M1-M12.

**Offline.** The requirements' edge case reads: "The harness is offline or the tailnet is
unreachable: show a clear offline state and allow retry without losing the drafted prompt."
This is distinct from M7 and is not a re-claim of it — M7 surfaced *documented harness error
codes* returned by a reachable service, whereas this is a transport failure where no response
exists at all. A drafted prompt discarded on a failed send is the worst version of this: the
human retypes into a phone keyboard.

**Long transcripts.** "A very long transcript must not break rendering." There is a concrete
latent defect behind this rather than a hypothetical: `dom-target.ts`'s `paint` calls
`transcriptList.replaceChildren(...transcriptItems)`, rebuilding every transcript node on
every paint. Once M12 subscribes paint to streaming deltas, a long conversation does O(turns)
DOM work per delta — so the cost is O(turns x deltas) across a single generation, on the
slowest device in the system. The criterion below is written to be falsifiable against that
specific shape rather than as a vague performance wish.

**Every acceptance criterion here is derived.** All eleven ACs are owned by M1-M11; this
milestone proves none of them and must never be recorded as covering one.

**It runs after M12**, which builds the controls and the subscription this milestone hardens.

### Architecture

C10, C12

**Realised this phase: C10 (UI) only.** Every change landed in `web/src/ui/` — the view model
and the DOM target. **C12 (PWA Shell) was not touched.** The field above was a planning
estimate written before the work was broken down; on inspection both edge cases are
view-layer concerns and neither required a shell change. This is not an architecture
deviation — no component boundary, technology choice or ownership changed, and nothing was
built outside the seams `architecture.md` defines — so no entry was added to that file's
`## Deviations`. It is recorded here so the reviewer does not go looking for C12 evidence
that does not exist.

Note also what was deliberately NOT done: the transport half of the offline path
(`HarnessOfflineError` in C7, and the `draftPrompt` attachment in C9) already existed from
M7 and was left unmodified. Both files were forbidden in the task packets.

### As-Built

`.harness/as-built/M13.md` — RECORDED, no claim mismatches. C10 only, matching this
milestone's own "realised C10, not C12" note; 2 of 6 changed files attributed to a component,
5 edges observed.


### Acceptance Criteria
- [x] (edge case: offline) With the transport failing at the network layer — no HTTP
      response, injected rather than simulated by an error code — the view renders a distinct
      offline state that is not the generic `error` display and names no harness error code,
      and the drafted prompt is still present in the input afterwards.
- [x] (edge case: offline) A retry control re-sends the preserved draft without the human
      retyping it, and a retry that succeeds against the live harness proceeds to a normal
      `complete`. The failing attempt leaves no user or assistant turn behind in the local
      transcript.
- [x] (edge case: long transcript) With a transcript of at least 500 turns mounted, applying
      200 streaming deltas creates a number of DOM elements per delta that does not scale
      with transcript length — asserted by counting element creation across two transcript
      sizes an order of magnitude apart, so the assertion fails if `paint` still rebuilds
      every node.
- [x] (edge case: long transcript) The same 500-turn transcript renders every turn's text
      intact — no truncation, no reordering, no dropped turns — verified against the
      view-model's own ordering rather than against the renderer's output alone.
- [x] **Human-attested on the physical iPhone (`iphone-15-pro`).** With the phone in airplane
      mode or off the tailnet, sending shows the offline state rather than a hang or a
      generic failure, and the drafted prompt survives. **Not agent-provable** — no agent may
      check this box.

### Baseline

`052db73fb28be6aff2699a57b5bdf2678d286fe9` on branch `main` (recorded at the start of M13's
implementation phase, before any task ran).

### Evidence

Baseline for every diff below: `052db73`. All work is uncommitted in the working tree.

**Tasks, tier routed, and outcome:**

```
T0 — archive M12a-ii out of milestones.md     Cheap, attempt 1, PASS
T1 — distinct offline state, draft preserved,
     retry control                             Mid, attempt 3, PASS
     routed Mid: NOT LOW RISK — the defect (a silently discarded draft) is
     invisible to a superficially passing test, and the existing code cleared
     the input BEFORE the failure was known.
T2 — incremental transcript reconciliation     Mid, attempt 3, PASS
     routed Mid: NOT LOW RISK — a keyed DOM reconciliation whose failure modes
     (reorder, drop, stale node) are silent, and where a naive "only touch the
     last node" fix passes the performance assertion while corrupting the
     transcript.
```

No task needed a retry; no ladder was climbed. Task packets:
`.harness/tasks/M13-T0-archive-m12a-ii.md`, `M13-T1-offline-state.md`,
`M13-T2-incremental-transcript.md`.

**Source files changed**

- `web/src/ui/view-model.ts` — `GenerationDisplay` gains
  `| { kind: "offline"; draftPrompt: string | null }` as a **peer** of the `error` variant.
  It carries no `code` field, so an offline state structurally cannot surface a harness
  error code. `describeError` is unchanged.
- `web/src/ui/dom-target.ts` — four changes:
  - a dedicated `data-testid="offline"` section rendering hand-written copy
    (`OFFLINE_MESSAGE`), never routed through `describeError` and never reading
    `guidance.code`; `generationStatusText` returns plain `"Offline"`, so the generic
    `data-testid="status"` element never reads `Error (offline): ...`;
  - a `data-testid="retry"` button, present only while the state is offline, wired to a
    shared `doSend(conversationId, prompt)` extracted from the send handler, so retry
    re-sends the preserved draft down the identical path;
  - `promptInput.value = ""` moved into the success (`.then`) branch. It previously ran
    unconditionally as the handler's last statement — i.e. before the send promise settled.
    That was the draft-loss defect;
  - the transcript block no longer calls `transcriptList.replaceChildren(...)` over freshly
    built nodes. A persistent `transcriptNodes` array is reconciled per index: reuse the
    existing `<li>`, write `textContent` only when it actually differs, append only for new
    indices, and remove + truncate trailing nodes when the transcript shrinks. The
    transcript `<ul>` element itself is never replaced, preserving the positional selectors
    several existing tests rely on.

**Test files changed** — `test/web/ui/dom-target.test.ts`, `test/web/ui/view-model.test.ts`.
Suite went 846 -> 852 tests. No existing test was weakened, skipped or deleted; each task's
verifier checked this specifically.

**Harness state changed** — `.harness/milestones.md` (this record, plus the M12a-ii
archiving) and `.harness/archive/M12a-ii.md` (new, 612 lines).

### Validation

Bun 1.4.0 at `~/.bun/bin/bun`, not on PATH in non-interactive shells.

| Command | Exit | Result |
| --- | --- | --- |
| `bun test` (full suite) | 0 | 852 pass, 0 fail, 6706 expect() calls, 32 files |
| `bun x tsc --noEmit` | 0 | clean, no errors |
| `bun test test/web/ui/` | 0 | 155 pass, 0 fail |

Every task's validation was re-run by an independent verifier rather than accepted from the
worker. The orchestrator additionally re-ran `bun x tsc --noEmit` itself (exit 0) because the
first verifier reported the type-check as a conclusion without a command and an exit status,
and re-ran the full suite after the T2 verifier's revert experiment to confirm the tree was
left as found (852 pass, exit 0, `transcriptList.replaceChildren` confirmed absent).

**The falsifiability check on the long-transcript criterion — the evidence that matters most
here.** Acceptance criterion 3 is a performance assertion, and one that passes against the
unfixed code proves nothing. The T2 verifier therefore reverted the transcript block to the
old `replaceChildren` rebuild and ran the new test against it independently:

```
old code:   50 turns ->  10,200 element creations over 200 deltas
           500 turns -> 100,200 element creations over 200 deltas
           scaling test FAILS:  Expected: < 20   Received: 90000
new code:   50 turns ->     601        500 turns -> 601
           identical regardless of transcript length
```

The measurement drives the real paint path (`handle.setStreamingText` through the mount
controller), not a hand-built view model passed straight to `paint`, and counts creations by
instrumenting `document.createElement` before `createDomTarget` captures the document.

Criterion 4's fidelity test builds 500 turns with distinguishable per-turn content
(`turn-${i}-of-${turnCount}-unique-payload`) and compares the rendered `<li>` list entry by
entry, in order, against `buildViewModel(state).transcript` — the view model's own ordering,
not the renderer's output. Companion tests cover the cases an index-keyed reconciliation
could break: a conversation switch (whole transcript replaced), a pending entry becoming a
real turn, and the `(cancelled)` marker.

The offline failure is injected at the **network layer**, as criterion 1 demands — a real
`createApiClient` over a `fetch` that rejects with `TypeError("Failed to fetch")`, driving a
real `createSessionCoordinator` — not a fabricated `{code: "offline"}` response.

**Not validated in the implementation phase — the live-harness clause is now CLOSED in
review cycle 1.** The implementation phase recorded that the harness was unreachable
(`OPENWEIGHT_HARNESS_BASE_URL` unset, "a probe of the default returned `000`"). **That
finding was wrong**, and the cycle-1 reviewer said so. Re-probed at the start of the fix
cycle, both `http://127.0.0.1:7787/v1/profiles` and the default base URL
`https://ryans-mac-studio.tailc3648a.ts.net/v1/profiles` return HTTP `401` — the harness is
up and simply requires auth. This machine is the Mac Studio. The `000` reading was an
environmental misdiagnosis, not an environmental constraint, and it caused the milestone's
strongest clause to be waved through unproven.

Correction task M13-C2 closed it: `src/host/m13-proof.ts` + `proof:m13` now prove the clause
against the live harness, following the same per-milestone convention as M2-M12b. See the
`### Review` section below for the runs.

**Still outstanding:** **criterion 5 only**, which is human-attested by construction and
which no agent may check.

### Review

**Cycle 1 — verdict CHANGES REQUIRED. Review tier: `sonnet`.**
Report: `.harness/reviews/M13-cycle1.md`.
Correction diff: `.harness/reviews/M13-cycle1.patch`.
Pre-correction snapshot: `f5a199b9c6a8facc772e16140199905cc756bfac`.
Post-correction snapshot: `ee8c752b671cb056cc9ab4640ed1976f1afbb89f`.
(Both are unreachable `commit-tree` snapshots that capture untracked work; the patch between
them is the durable artifact. The four M13 source files are untracked, so no `git diff`
against `### Baseline` shows them — the patch is the only correct view of this cycle.)

The reviewer marked criteria 1-4 PASS with per-criterion evidence and independently
reproduced criterion 3's falsifiability against the pre-M13 renderer. Criterion 5 remains
OUTSTANDING and unchecked, awaiting the human. **No acceptance-criterion box is checked
here** — that is for a passing review to drive, not this cycle.

**Findings resolved this cycle**

| Finding | Severity | Correction task | Tier + outcome |
| --- | --- | --- | --- |
| Stray `web/src/ui/dom-target.ts.bak` (pre-M13 renderer) left in the source tree, contradicting the milestone's "tree left as found" claim | IMPORTANT | M13-C1 | Cheap, attempt 1, PASS |
| Live-harness clause of criterion 2 unproven; no `m13-proof.ts`, breaking the per-milestone live-proof convention | IMPORTANT | M13-C2 | Mid, attempt 3, PASS — routed Mid: NOT CLEARLY SPECIFIED and NOT EASILY VERIFIED (where to inject and withdraw a transport failure inside the real streaming client is not derivable from the packet, and a live-network proof can pass vacuously by never reaching the harness) |
| `promptInput.value = ""` on the success path discards text typed while a send is in flight | OPTIONAL | M13-C3 | Cheap, attempt 1, PASS — **taken, not deferred** |

Correction packets: `.harness/tasks/M13-C1-remove-bak.md`,
`.harness/tasks/M13-C2-live-harness-proof.md`,
`.harness/tasks/M13-C3-prompt-input-snapshot.md`.

**What each correction did**

- **C1** — deleted `web/src/ui/dom-target.ts.bak`; added `*.bak` to `.gitignore` so a future
  revert-and-measure experiment cannot leave the same debris. `web/src/ui/dom-target.ts` was
  proven untouched by md5 before and after (`e9e124a0...` both sides).
- **C2** — added `src/host/m13-proof.ts` (407 lines) and `"proof:m13"` to `package.json`.
  It drives the shipped UI's own DOM under happy-dom (clicks `data-testid="send"` and
  `data-testid="retry"`) through the real `createApiClient` / `createConversationStore` /
  `createSessionCoordinator` / `createDomTarget` / `mount`, against the live harness at
  `resolveBaseUrl()` with the real token: injects `TypeError("Failed to fetch")` on the
  `generate` call only, asserts the offline section renders with no harness error code and
  the draft survives, asserts **zero** turns leaked, withdraws the injection, retries, and
  asserts a live `complete` with telemetry read from the harness's own SSE stream — plus
  exactly one user and one assistant turn, and that the retried request body carried the
  preserved draft rather than a retype. **No production code changed to make it pass.**
- **C3** — `doSend` now snapshots `promptInput.value` at entry and clears it in the success
  branch only if the value is still identical, so text typed during an in-flight send or
  retry survives. Two new tests; suite 852 -> 854.

**Files the corrections changed**

| File | Named by a finding? |
| --- | --- |
| `web/src/ui/dom-target.ts.bak` (deleted) | yes — finding 1 |
| `web/src/ui/dom-target.ts` | yes — finding 3 (`dom-target.ts:186-194`) |
| `src/host/m13-proof.ts` (new) | yes — finding 2 named it as the missing artifact |
| `.gitignore` | **NO — not named as a file to change.** Finding 1 observed only that the `.bak` was "Not listed in `.gitignore`"; adding `*.bak` is the orchestrator's own hardening beyond the suggested correction, which was just "delete the file". |
| `package.json` | **NO — not named.** Implied by finding 2's "and a `proof:m13` script", but the file itself was never cited. One line added after `proof:m12b`; nothing else in the file changed. |
| `test/web/ui/dom-target.test.ts` | **NO — not named.** C3's two new tests. No existing test touched. |
| `.harness/tasks/M13-C{1,2,3}-*.md` (new) | harness bookkeeping, not source |

**Three files outside the findings' named set were touched** (`.gitignore`, `package.json`,
`test/web/ui/dom-target.test.ts`), recorded here explicitly because the next review's scope
depends on it. All three are additive and mechanical, but that scope decision belongs to the
reviewer, not to this record.

**Validation, re-run by the orchestrator itself and not credited from any worker**

| Command | Exit | Result |
| --- | --- | --- |
| `bun x tsc --noEmit` | 0 | clean |
| `bun test` (full suite) | 0 | 854 pass, 0 fail, 6710 expect() calls, 32 files |
| `bun run proof:m13` (live) | 0 | `M13 LIVE PROOF: PASS`, all 6 assertions |
| `find web src test scripts -name '*.bak' -o -name '*.orig' -o -name '*.rej'` | 0 | no output — tree is clean |

Each correction was additionally re-run by an independent verifier. Three checks worth
naming, because they are what makes the above more than a claim:

- **C3 falsifiability, reproduced by the verifier**: it copied `dom-target.ts` out of the
  repository, reverted only the guard, and saw the new test fail with
  `Expected: "new text typed while sending" / Received: ""`, then restored the file and
  confirmed by md5 (`d3a94845...` both sides) and 854/0. The test genuinely discriminates.
- **C2 falsifiability, reproduced by the verifier**: run once with
  `OPENWEIGHT_HARNESS_BASE_URL=http://127.0.0.1:1`, the proof exits **1** with
  `FAIL - Setup: could not reach the live harness`. It cannot pass vacuously by skipping.
- The live proof was run **three times by three separate contexts** (worker, verifier,
  orchestrator) against the real harness, returning different conversation ids and different
  real telemetry each time (`tokens_per_second` 173.27 / 179.68 / 161.39). It is talking to
  the model, not to a fixture.

The first verifier reported `tsc` as a conclusion without an exit status — the same defect
this record noted in the implementation phase — so the orchestrator re-ran it directly
(exit 0). Recorded so the pattern stays visible rather than being smoothed over.

**Cycle 2 — verdict PASS. Review tier: `sonnet`.**
Scope was **widened back to the whole milestone**, not scoped to the correction patch: the fix
cycle changed three files no cycle-1 finding named (`.gitignore`, `package.json`,
`test/web/ui/dom-target.test.ts`), which falsifies the "nothing else changed" premise a narrow
cycle-2 scope rests on.

A fresh reviewer graded all five criteria and returned **no findings at any severity** —
no BLOCKER, no IMPORTANT, no OPTIONAL. It re-ran validation itself rather than crediting the
record (`bun x tsc --noEmit` exit 0; `bun test` 854 pass / 0 fail; `bun run proof:m13` exit 0
with live telemetry `tokens_per_second=155.87`), and it re-established the two falsifiability
properties independently:

- **`proof:m13` cannot pass vacuously** — re-run against an unreachable base URL
  (`OPENWEIGHT_HARNESS_BASE_URL=http://127.0.0.1:1`) it exits **1** at setup rather than
  reporting success.
- **Criterion 3's performance assertion is genuinely falsifiable** — reproduced *without* the
  `.bak` that cycle 1 relied on and that C1 deleted, by hand-reverting only the transcript block
  to `replaceChildren`: 2 tests fail, including the exact scaling assertion
  (`Expected: < 20, Received: 90000`). The tree was restored and md5-confirmed.

It also confirmed C3 did not weaken criterion 1: the offline `.catch()` branch never touched
`promptInput.value` in either the pre- or post-C3 code, so the failure-path draft guarantee was
never routed through the new snapshot-compare guard. No test was skipped, weakened or deleted
(`.skip(`/`.only(`/`xit(`/`xdescribe(` all absent). No architectural drift: all source changes
stayed inside C10's `web/src/ui/` boundary, and `src/host/m13-proof.ts` follows the pre-existing
host-proof convention rather than introducing a component.

**The review cap is now spent** — two cycles recorded, cycle 2 passed with nothing open. There is
nothing to route and no third review is permitted.

### Human Attestation — RECORDED

**Criterion 5 was attested by the human on 2026-09-02 at 16:06 local time, on the physical
iPhone (`iphone-15-pro`), in the installed home-screen app.** Recorded from the human's own
observation and two screenshots they supplied; no agent performed, inferred or checked it
before that. Bundle served at the time: `web/dist` built 2026-09-02 13:44, after the last M13
source change (13:38), containing the offline and retry code.

**Observed — airplane mode (screenshot 1, status bar shows the airplane indicator, 16:06):**

- The draft `Test` was **still present in the prompt input** after the failed send.
- A distinct section headed **`Offline`** rendered, with the milestone's own hand-written copy:
  "Your Mac could not be reached. Your prompt has been kept — use Retry to send it again once
  the harness is reachable."
- **No harness error code appeared** — no `http_502`, no `Unexpected harness error`, and no
  generic error display. No hang and no unresolving spinner.
- A **`Retry`** control was present.
- **No turns leaked into the transcript** — the conversation showed no user and no assistant
  turn from the failed attempt.

**Observed — network restored (screenshot 2, status bar shows 4G, 16:06):**

- Tapping **Retry** sent the preserved prompt with **no retyping**; the input was empty
  afterwards.
- The transcript showed exactly **one user turn (`Test`) and one assistant turn**, and the
  generation reached a normal terminal state: `Complete — 60.704031871865034 tok/s, 27 tokens
  evaluated`.

This is the same attestation contract M10, M12 and M12a-ii used. With criteria 1-4 proven,
reviewed across two cycles and checked, and criterion 5 now attested, the milestone is `DONE`.

### Review Cycles
1

### Follow-ups

- **Sequencing hazard with `M12c`, recorded 2026-09-02 — do not run these two concurrently.**
  This milestone's long-transcript criteria measure element creation in the same renderer that
  `M12c` rewrites into chat bubbles. Whichever runs second inherits the other's layout, and
  running them in parallel would have each measuring a renderer the other is changing. `M12c`
  carries its own narrow incremental-growth guard so it cannot regress what is measured here,
  but that guard is not a substitute for these criteria — it asserts only that growth is in
  place, not that it is independent of transcript length.


- **Observed during the criterion-5 attestation, recorded not implemented — the completion
  telemetry line renders `quantization Q4_K_M` and `context limit 262144`.** Screenshot 2 of
  the attestation shows it. This is outside M13's criteria and predates it (the telemetry
  display is M2's), so nothing was changed here. It matters because the requirements added on
  2026-09-02 name it directly: **FR12** states "Physical model names, sizes, digests and
  **quantization tags** never reach the UI", and **AC17** requires that no physical model
  identity appear anywhere in the rendered UI. Whichever milestone takes FR12 must treat this
  as existing UI to correct, not only as a rule for new UI to follow.

- ~~**Criterion 5 is OUTSTANDING and awaits the human.**~~ **CLOSED — attested by the human
  on 2026-09-02 at 16:06 on `iphone-15-pro`.** No agent checked, claimed or produced evidence
  for it; the record above is the human's own observation. See `### Human Attestation —
  RECORDED`.
- ~~**The live-harness clause of criterion 2 is UNPROVEN this phase.**~~ **CLOSED in review
  cycle 1** by correction task M13-C2. `src/host/m13-proof.ts` and `proof:m13` now exist and
  pass against the live harness, restoring the per-milestone live-proof convention that
  M2-M12b follow. The premise of this follow-up — that the harness was down — was false: it
  was reachable in the same environment the whole time, returning `401` rather than `000`.
  **Kept rather than deleted, as the lesson:** a single unauthenticated probe reading `000`
  was treated as "the Mac's harness is not running" and used to excuse the milestone's
  strongest acceptance clause. A probe that cannot distinguish "down" from "needs auth" is
  not evidence of absence, and an environmental excuse should be re-probed before it is
  allowed to downgrade a criterion.
- The conversations list and profiles list in `dom-target.ts` are still rebuilt wholesale on
  every paint (`replaceChildren` over freshly built nodes). They contribute a constant ~3
  element creations per paint and do NOT scale with transcript length, so they do not affect
  criterion 3 — which is why they were left alone under scope discipline. Worth the same
  treatment if the conversation list ever grows large.
- The `M12c` sequencing hazard above is **inert**: M12c was deferred by the human and not
  implemented, so M13 measured the pre-UI-pass renderer as intended and landed its
  incremental-growth guard first. A future UI pass therefore arrives on already-protected
  code, and should re-run criterion 3's scaling test against its own renderer.
- **The cycle-1 OPTIONAL finding was TAKEN, not deferred** (M13-C3). Recorded so the decision
  is visible: the reviewer rated it OPTIONAL and outside M13's stated criteria, but it is the
  same class of silent-draft-loss defect the milestone exists to eliminate, the fix is a
  four-line guard behind two discriminating tests, and it touches a file a finding already
  named. Nothing about it is left outstanding.
- **`.harness/milestones.md` is over the ~400-line archiving threshold** (~1150 lines after
  this cycle). Archiving was deliberately NOT done during a fix cycle — restructuring the
  state file while a milestone is mid-review moves the very record the next review reads. It
  should be done at the start of the next implementation phase, and M13 itself must not be
  archived until it is settled.
